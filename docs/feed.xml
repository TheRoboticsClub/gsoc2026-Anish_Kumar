<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/feed.xml" rel="self" type="application/atom+xml"/><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-22T17:18:27+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/feed.xml</id><title type="html">blank</title><subtitle>GSoC 2026 weekly blog — Drone Cat-Mouse Chase Exercise with JdeRobot Robotics Academy. </subtitle><entry><title type="html">Moving from N Users to N Robots</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-support/" rel="alternate" type="text/html" title="Moving from N Users to N Robots"/><published>2026-06-22T06:30:00+00:00</published><updated>2026-06-22T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-support</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-support/"><![CDATA[<p>This week the work shifted from supporting multiple users to supporting multiple robots — making RAM symmetric on both sides. Here’s where things stand.</p> <hr/> <h2 id="where-we-started-n-applications-agentgroup">Where we started: N applications (AgentGroup)</h2> <p>A couple of weeks back I added support for running multiple user applications at once — the cat-and-mouse exercise needs two programs running together (the cat the student writes, and a pre-programmed mouse). Instead of teaching RAM’s state machine about “two agents,” I wrapped the agent processes in a small <code class="language-plaintext highlighter-rouge">AgentGroup</code>. The state machine still fires one trigger — play, pause, reset, stop — and the group fans that operation out over every process inside it.</p> <p>So one application or five, the lifecycle code is the same: <code class="language-plaintext highlighter-rouge">pause_all</code>, <code class="language-plaintext highlighter-rouge">resume_all</code>, <code class="language-plaintext highlighter-rouge">kill_all</code>. The finite state machine never had to change. That kept things clean and meant single-agent exercises kept working exactly as before.</p> <hr/> <h2 id="cleaning-up-the-pr-review">Cleaning up the PR review</h2> <p>The cat-and-mouse PRs went through review and a few good points came back from the maintainers. The big one: the drone was being defined inline inside the world file (hundreds of lines), and the mentors wanted it as its own model that the world just includes.Fixed moved the drone model out into its own files, and the world file dropped from ~370 lines to ~75.</p> <p>There was also a round of merge conflicts as upstream kept moving — mostly database id clashes (a new exercise upstream grabbed the same id as mine), which I resolved by bumping the cat-mouse ids out of the way.</p> <hr/> <h2 id="now-n-robots">Now: N robots</h2> <p>RAM handles N user programs, but robots were still added by hand, one at a time, in the world file. So I started on the robot side.</p> <p>The approach mirrors the agent one, but the mentors suggested keeping it as a list rather than a wrapper class: turn the single robot launcher + config into a list of robot launchers + configs. Each robot in the list has its own entity, <code class="language-plaintext highlighter-rouge">launch_file_path</code>, and <code class="language-plaintext highlighter-rouge">start_pose</code>, while the robot type stays shared across all of them. The manager just loops the list. And since spawning N robots one by one is slow, the spawn runs in parallel — a thread per robot.</p> <hr/> <h2 id="the-tricky-part-gazebo-reset">The tricky part: Gazebo reset</h2> <p>The interesting problem here is reset. If you spawn a robot into Gazebo at runtime and then call the world reset, Gazebo wipes anything that wasn’t in the world at load time — so the robot disappears and never comes back. And you can’t reset the robot entity directly either; doing that segfaults.</p> <p>The clean answer is already baked into how RAM is structured: robots are spawned by the <code class="language-plaintext highlighter-rouge">robot_launcher</code>, and RAM re-runs the <code class="language-plaintext highlighter-rouge">robot_launcher</code> on reset. So reset becomes: kill each robot’s launcher → reset the (now empty) world → relaunch each robot. The robot comes back fresh, no direct entity reset, no segfault. Moving from one robot launcher to a list of them means this same proven reset flow just loops over all N robots — nothing about the logic changes, it only fans out.</p> <hr/> <h2 id="whats-next">What’s next</h2> <ul> <li>The robots config comes from the database — so the next piece is letting a universe describe several robots (shared type, per-robot entity + start pose) and the backend handing RAM a robots list.</li> <li>Testing with the F1 car on a circuit (simpler than drones — no full autonomy stack), spawning two cars and checking play / pause / reset all behave for both. The end goal is full symmetry — N applications running against N robots, all driven from config, with the state machine left untouched.</li> </ul>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><summary type="html"><![CDATA[RAM now handles multiple user applications — this week the work shifted to making the robot side symmetric too.]]></summary></entry><entry><title type="html">Getting the Drone Cat-and-Mouse Exercise into Robotics Academy</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/drone-cat-mouse-integration/" rel="alternate" type="text/html" title="Getting the Drone Cat-and-Mouse Exercise into Robotics Academy"/><published>2026-06-13T06:30:00+00:00</published><updated>2026-06-13T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/drone-cat-mouse-integration</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/drone-cat-mouse-integration/"><![CDATA[<p>I’ve been working on this integration for the past few weeks — getting the drone cat-and-mouse exercise properly wired into Robotics Academy has been one of the bigger pieces of the project. Along the way I ran into a bunch of bugs, some obvious and some not at all, and I wanted to document them here as I worked through them.</p> <p>The goal was straightforward: get the exercise running inside Robotics Academy, wired into RAM, launchable from the UI. In reality that meant seeding the database, fixing the launch files, and working through bugs that only reveal themselves once both drones are actually running at the same time.</p> <p>Here’s what it looks like now:</p> <video width="100%" controls=""> <source src="/gsoc2026-Anish_Kumar/assets/video/drone_cat_chase.webm" type="video/webm"/> </video> <hr/> <h2 id="how-exercises-actually-plug-into-ram">How exercises actually plug into RAM</h2> <p>Before getting into the bugs, it helps to understand how Robotics Academy works under the hood. You don’t run exercise code directly — the browser uploads a zip to RAM, and RAM handles everything: launch the world, spawn the robots, start the GUI tools, then run your code as a managed process.</p> <p>For cat-and-mouse I used RAM’s two-process model that we built in the previous weeks. The <strong>cat</strong> is the student’s code (<code class="language-plaintext highlighter-rouge">academy.py</code>), and the <strong>mouse</strong> is a pre-programmed agent RAM launches from a <code class="language-plaintext highlighter-rouge">processB/</code> folder. RAM extracts the zip into <code class="language-plaintext highlighter-rouge">/workspace/code</code>, starts <code class="language-plaintext highlighter-rouge">academy.py</code> as agentA, and if <code class="language-plaintext highlighter-rouge">processB/academy.py</code> exists it starts that as agentB.</p> <h2 id="getting-the-database-to-recognise-the-exercise">Getting the database to recognise the exercise</h2> <p>The first step before anything else is seeding the database — if Robotics Academy doesn’t know the exercise exists, nothing else matters. I wrote an idempotent <code class="language-plaintext highlighter-rouge">copy_drone_cat_mouse.sh</code> script that creates the full chain of linked rows: an <strong>exercise</strong> row (<code class="language-plaintext highlighter-rouge">id=27</code>, <code class="language-plaintext highlighter-rouge">exercise_id='drone_cat_mouse'</code>, tagged <code class="language-plaintext highlighter-rouge">MULTI-AGENT</code>), a <strong>world</strong> row pointing at the launch file, a <strong>universe</strong> row linking the world, an <strong>exercises_universes</strong> row marking it the default, and <strong>exercises_tools</strong> enabling the console, simulator, and web GUI. The script ends with a join query that prints the whole chain so I can see in one glance that the exercise actually resolves to the right launch file.</p> <h2 id="one-rule-that-kept-things-sane">One rule that kept things sane</h2> <p>Don’t touch the shared packages. <code class="language-plaintext highlighter-rouge">drone_wrapper.py</code>, the RAM state machine, and the AS2 stack are used by every other drone exercise. Any change I made there could break something that was already working. So I kept all exercise-specific logic inside cat-and-mouse’s own <code class="language-plaintext highlighter-rouge">HAL.py</code> files and made any RAM edits exercise-agnostic — changes that work for N agents and any world name, not changes hardcoded for this one exercise.</p> <hr/> <h2 id="the-bugs-in-the-order-they-hit-me">The bugs (in the order they hit me)</h2> <h3 id="the-second-drone-was-driving-the-first-drones-motors">The second drone was driving the first drone’s motors</h3> <p>Both drones armed. Both showed FLYING. Rotors on both were spinning. But drone1 just sat there on the ground and didn’t move.</p> <p>I ran <code class="language-plaintext highlighter-rouge">ros2 node info /drone1/platform</code> and looked at its publishers. It was publishing to <code class="language-plaintext highlighter-rouge">/gz/drone0/cmd_vel</code> — drone0’s Gazebo plugin — not its own <code class="language-plaintext highlighter-rouge">/gz/drone1/cmd_vel</code>. Drone1 was working fine, it was just sending all its effort to the wrong place.</p> <p>After digging through the aerostack2 launch files and spending some time searching through ROS2 launch system docs and community threads, I found the cause. The <code class="language-plaintext highlighter-rouge">platform_gazebo_launch.py</code> declares <code class="language-plaintext highlighter-rouge">cmd_vel_topic</code> via <code class="language-plaintext highlighter-rouge">DeclareLaunchArgument</code> inside an <code class="language-plaintext highlighter-rouge">OpaqueFunction</code>. What I didn’t realise is that all included launch files share one global <code class="language-plaintext highlighter-rouge">LaunchContext</code> — it’s essentially a single dictionary for the entire launch tree. <code class="language-plaintext highlighter-rouge">DeclareLaunchArgument</code> behaves like <code class="language-plaintext highlighter-rouge">dict.setdefault</code>: it only sets the value if the key doesn’t already exist. So drone0 runs first, writes <code class="language-plaintext highlighter-rouge">cmd_vel_topic = /gz/drone0/cmd_vel</code> into the dict, and when drone1 tries to register its own value, the key already exists and the assignment is silently skipped. Drone1 ends up with drone0’s topic.</p> <p>This is genuinely easy to miss because the ROS2 runtime namespace system (what gives you <code class="language-plaintext highlighter-rouge">/drone0/</code> and <code class="language-plaintext highlighter-rouge">/drone1/</code> isolated topics and nodes) works completely correctly. It’s only the launch argument dict that has this shared-global behaviour. Most people never hit it because multi-robot setups usually run each robot as a separate <code class="language-plaintext highlighter-rouge">ros2 launch</code> command — separate processes, separate dicts, no collision.</p> <p>Fix: I modified <code class="language-plaintext highlighter-rouge">as2_default_gazebo_sim.launch.py</code> to launch <code class="language-plaintext highlighter-rouge">as2_platform_gazebo_node</code> directly from inside an <code class="language-plaintext highlighter-rouge">OpaqueFunction</code>, computing the topic names as plain Python strings and passing them straight into <code class="language-plaintext highlighter-rouge">Node(parameters=[...])</code>. They never enter the global launch dict at all. I’m also raising this upstream as a scalability issue in aerostack2’s Humble launch files.</p> <hr/> <h3 id="the-mouse-process-died-immediately-on-import">The mouse process died immediately on import</h3> <p>As soon as RAM launched agentB, it crashed: <code class="language-plaintext highlighter-rouge">ModuleNotFoundError: No module named 'hal_interfaces'</code>.</p> <p>I checked the extracted folder layout. <code class="language-plaintext highlighter-rouge">hal_interfaces</code> lives at <code class="language-plaintext highlighter-rouge">/workspace/code/</code> — but <code class="language-plaintext highlighter-rouge">processB/academy.py</code> runs from <code class="language-plaintext highlighter-rouge">/workspace/code/processB/</code>. Python adds the script’s own directory to <code class="language-plaintext highlighter-rouge">sys.path</code>, not its parent. From <code class="language-plaintext highlighter-rouge">processB/</code>, the shared commons in the parent folder are invisible.</p> <p>Fix: in <code class="language-plaintext highlighter-rouge">manager.py</code> I inject <code class="language-plaintext highlighter-rouge">PYTHONPATH=/workspace/code</code> into agentB’s environment when creating its <code class="language-plaintext highlighter-rouge">Popen</code>. Same import experience as agentA, no changes to the exercise files themselves.</p> <hr/> <h3 id="takeoff-was-throwing-task-got-bad-yield">Takeoff was throwing “Task got bad yield”</h3> <p>Both agents were crashing during <code class="language-plaintext highlighter-rouge">takeoff()</code> with a <code class="language-plaintext highlighter-rouge">RuntimeError: Task got bad yield</code>. The traceback pointed at <code class="language-plaintext highlighter-rouge">asyncio.run()</code> wrapping a state-machine service call. rclpy Futures use <code class="language-plaintext highlighter-rouge">yield self</code> in their <code class="language-plaintext highlighter-rouge">__await__</code>, which asyncio’s event loop doesn’t know how to drive.</p> <p>Fix: in the exercise’s <code class="language-plaintext highlighter-rouge">HAL.py</code> I wrote a small <code class="language-plaintext highlighter-rouge">_call_state_event()</code> that calls the service with <code class="language-plaintext highlighter-rouge">call_async()</code> and polls <code class="language-plaintext highlighter-rouge">future.done()</code> in a plain loop, letting DroneWrapper’s 20 Hz spin thread do the resolving. No asyncio, no crash. The shared <code class="language-plaintext highlighter-rouge">DroneWrapper</code> stays untouched.</p> <hr/> <h3 id="the-cat-reached-flying-but-never-moved-toward-the-mouse">The cat reached FLYING but never moved toward the mouse</h3> <p>This one took the longest to figure out.</p> <p>The cat armed, took off, reached the correct altitude, state machine said FLYING — and then just hovered there. <code class="language-plaintext highlighter-rouge">ros2 topic hz /drone0/motion_reference/twist</code> showed nothing being published. <code class="language-plaintext highlighter-rouge">ros2 topic echo /drone0/controller/info</code> showed the controller stuck in HOVER mode. The process wasn’t dead — I checked <code class="language-plaintext highlighter-rouge">/proc/&lt;pid&gt;/task/*/wchan</code> and the main thread was just sleeping in its loop.</p> <p>To narrow it down, I killed the cat code entirely and ran a small standalone script: create a fresh <code class="language-plaintext highlighter-rouge">DroneWrapper("drone0")</code>, call <code class="language-plaintext highlighter-rouge">set_cmd_vel</code>. It switched to SPEED mode in 0.7 seconds, no problem. So the drone stack was fine. The problem was something specific to the cat exercise setup. The only thing cat had that the working mouse didn’t was a mouse-position subscriber.</p> <p>Here’s what was happening. When I added mouse tracking I gave it a dedicated <code class="language-plaintext highlighter-rouge">MultiThreadedExecutor</code> with <code class="language-plaintext highlighter-rouge">.spin()</code> running in its own thread — the reasoning being “the pose topic publishes at ~80 Hz, better give it a dedicated worker so it never misses an update.” In C++ that would be reasonable. In Python, the GIL means only one thread runs Python at a time. <code class="language-plaintext highlighter-rouge">MultiThreadedExecutor.spin()</code> is a blocking, full-speed loop with no sleep. So that thread was holding the GIL almost continuously, starving DroneWrapper’s 20 Hz background thread — the one that receives <code class="language-plaintext highlighter-rouge">controller/info</code> callbacks. AS2’s motion handler won’t publish velocity commands until its internal <code class="language-plaintext highlighter-rouge">current_mode_</code> (which gets updated by that callback) reads SPEED. With the callback starved, <code class="language-plaintext highlighter-rouge">current_mode_</code> stayed at HOVER, the mode-switch wait timed out after 5 seconds, and <code class="language-plaintext highlighter-rouge">set_cmd_vel</code> returned silently without doing anything. The “stuck in HOVER” symptom was a consequence, not the root cause.</p> <p>Fix: moved the mouse tracker onto the cameras’ existing executor, which uses <code class="language-plaintext highlighter-rouge">spin_once(timeout_sec=0) + time.sleep(1/30)</code>. It yields the GIL every cycle. DroneWrapper’s thread gets to run, <code class="language-plaintext highlighter-rouge">controller/info</code> flows, and <code class="language-plaintext highlighter-rouge">set_cmd_vel</code> publishes immediately. The lesson: it wasn’t the executor type that was the problem, it was the greedy <code class="language-plaintext highlighter-rouge">.spin()</code> with no yield.</p> <hr/> <h3 id="the-cat-was-chasing-a-fixed-point-in-space">The cat was chasing a fixed point in space</h3> <p>Once the cat was actually moving, it flew toward a single fixed location rather than tracking the mouse.</p> <p>I ran <code class="language-plaintext highlighter-rouge">ros2 topic echo /drone1/self_localization/pose</code> — nothing. Added <code class="language-plaintext highlighter-rouge">--qos-reliability best_effort</code> — data started printing. Then <code class="language-plaintext highlighter-rouge">ros2 topic info /drone1/self_localization/pose -v</code> confirmed it: the publisher was BEST_EFFORT. My subscriber was using the default QoS profile which is RELIABLE. Those two are incompatible; zero messages were delivered, and <code class="language-plaintext highlighter-rouge">get_mouse_position()</code> kept returning its initialised default coordinates.</p> <p>Fix: subscribe with <code class="language-plaintext highlighter-rouge">qos_profile_sensor_data</code> (BEST_EFFORT). After that the position updated live with every frame.</p> <hr/> <h3 id="stop-left-both-drones-coasting">Stop left both drones coasting</h3> <p>Pressing Stop in the GUI didn’t actually stop the drones — they kept moving on their last velocity command.</p> <p>The issue was in <code class="language-plaintext highlighter-rouge">on_terminate_application</code>: the order was kill the processes, then pause the simulator. Between those two steps the drones still had momentum from their last command and Gazebo’s physics kept running. Swapping the order — pause the sim first, then kill processes, then reset — freezes everything instantly at the moment Stop is hit. Works for any exercise, not just this one.</p> <hr/> <h3 id="pause-and-reset-were-silently-doing-nothing">Pause and Reset were silently doing nothing</h3> <p>Pause appeared to work but didn’t actually freeze the drones. Reset only reset drone0.</p> <p>I ran <code class="language-plaintext highlighter-rouge">gz service -l</code> and compared it against what the code was calling. The gz control calls in <code class="language-plaintext highlighter-rouge">launcher_gzsim.py</code> were hardcoded to <code class="language-plaintext highlighter-rouge">/world/default/control</code>. This exercise’s world is <code class="language-plaintext highlighter-rouge">my_city_world</code>. The calls were hitting a service that didn’t exist and failing silently. And <code class="language-plaintext highlighter-rouge">reset()</code> was hardcoded to call <code class="language-plaintext highlighter-rouge">/drone0/...</code> services — drone1 was completely ignored.</p> <p>Fix: I added <code class="language-plaintext highlighter-rouge">_find_drone_namespaces()</code> which scans <code class="language-plaintext highlighter-rouge">ros2 service list</code> for all <code class="language-plaintext highlighter-rouge">*/platform/state_machine/_reset</code> services and resets every one — it doesn’t care how many drones are running or what the world is named.</p> <hr/> <h2 id="files-changed">Files changed</h2> <ul> <li><code class="language-plaintext highlighter-rouge">RoboticsAcademy/exercises/drone_cat_mouse/python_template/HAL.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsAcademy/exercises/drone_cat_mouse/python_template/academy.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsAcademy/exercises/drone_cat_mouse/python_template/processB/{HAL.py,academy.py}</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsApplicationManager/.../manager/manager.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsApplicationManager/.../manager/launcher/launcher_gzsim.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsInfrastructure/jderobot_drones/launch/as2_default_gazebo_sim.launch.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsInfrastructure/Launchers/drone_cat_mouse.launch.py</code></li> </ul> <hr/> <h2 id="where-it-stands">Where it stands</h2> <p>The exercise launches cleanly from the Robotics Academy UI, both drones spawn and take off, the cat tracks and chases the live mouse, and Stop / Pause / Reset all behave correctly for both drones.</p> <p>Updated PRs with the latest commits:</p> <ul> <li><strong><a href="https://github.com/JdeRobot/RoboticsAcademy/pull/3872">RoboticsAcademy #3872</a></strong></li> <li><strong><a href="https://github.com/JdeRobot/RoboticsApplicationManager/pull/284">RoboticsApplicationManager #284</a></strong></li> <li><strong><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/742">RoboticsInfrastructure #742</a></strong></li> </ul>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="aerostack2"/><category term="debugging"/><category term="multi-agent"/><summary type="html"><![CDATA[Full integration of a two-drone chase exercise into RAM — database wiring, multi-process launch, and seven bugs debugged in order.]]></summary></entry><entry><title type="html">Multi-Process Code Execution in RoboticsAcademy</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/multiprocess-code-execution/" rel="alternate" type="text/html" title="Multi-Process Code Execution in RoboticsAcademy"/><published>2026-06-07T06:30:00+00:00</published><updated>2026-06-07T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/multiprocess-code-execution</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/multiprocess-code-execution/"><![CDATA[<p>This week’s goal was proving that RAM can manage two independent agent processes from a single exercise session — the foundation needed for the Drone Cat-Mouse exercise.</p> <hr/> <h2 id="why-the-browser-sends-the-zip">Why the browser sends the zip</h2> <p>One design decision worth noting: the exercise code — including <code class="language-plaintext highlighter-rouge">processB/</code> — is bundled and sent from the browser, not fetched from a database on the RAM side (which was one option considered).</p> <p>Keeping code in the browser payload means RAM stays decoupled from any storage backend — code lives with the client and gets pushed to RAM at run time. The mentor flagged this as a clean design point for RoboticsAcademy’s architecture.</p> <hr/> <h2 id="how-the-play-flow-works-now">How the Play flow works now</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/gsoc2026-Anish_Kumar/assets/img/architect_image-480.webp 480w,/gsoc2026-Anish_Kumar/assets/img/architect_image-800.webp 800w,/gsoc2026-Anish_Kumar/assets/img/architect_image-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/gsoc2026-Anish_Kumar/assets/img/architect_image.png" class="img-fluid rounded" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Zip merge architecture — how both agents land in /workspace/code/</figcaption> </figure> <p>The browser doesn’t just send the student’s code — it builds a <strong>finalZip</strong> by merging three sources:</p> <ol> <li><strong>userZip</strong> — the student’s <code class="language-plaintext highlighter-rouge">academy.py</code> from the browser editor (cat code, autosaved)</li> <li><strong>helperZip</strong> — fetched from the filesystem: <code class="language-plaintext highlighter-rouge">HAL.py</code>, <code class="language-plaintext highlighter-rouge">WebGUI.py</code>, and the entire <code class="language-plaintext highlighter-rouge">processB/</code> folder (pre-programmed mouse + its own <code class="language-plaintext highlighter-rouge">HAL.py</code>)</li> <li>The two zips are merged client-side into <strong>finalZip</strong> and sent to RAM</li> </ol> <p>RAM receives the merged payload, extracts everything to <code class="language-plaintext highlighter-rouge">/workspace/code/</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/workspace/code/
├── academy.py              ← student code (cat, drone0)
├── HAL.py                  ← drone0 interface
├── hal_interfaces/
├── gui_interfaces/
├── console_interfaces/
└── processB/
    ├── academy.py          ← pre-programmed mouse
    └── HAL.py              ← drone1 interface
</code></pre></div></div> <p>RAM then launches both:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 /workspace/code/academy.py           ← agentA (cat, drone0)
python3 /workspace/code/processB/academy.py  ← agentB (mouse, drone1)
</code></pre></div></div> <p>Both go into the <code class="language-plaintext highlighter-rouge">application_processes</code> dict. Pause sends SIGSTOP to both. Resume sends SIGCONT. Stop kills both.</p> <p>Verified both running simultaneously from their extracted paths:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>11255  python3 /workspace/code/academy.py
11256  python3 /workspace/code/processB/academy.py
</code></pre></div></div> <p>One process for the cat, one for the mouse — confirmed live. Each HAL resolves to a different drone namespace — same import name, different process context, different physical drone.</p> <p>Slowly heading towards the goal.</p> <hr/> <h2 id="what-this-means-for-the-project">What this means for the project</h2> <p>The pattern is exercise-agnostic. Any future exercise needing N agents follows the same structure: add a <code class="language-plaintext highlighter-rouge">processB/</code>, <code class="language-plaintext highlighter-rouge">processC/</code> folder to the exercise template. RAM detects and launches them. The <code class="language-plaintext highlighter-rouge">application_processes</code> dict handles lifecycle for all uniformly.</p> <hr/> <h2 id="pull-requests">Pull Requests</h2> <p>Also drafted and opened PRs this week consolidating all previous work:</p> <ul> <li><strong><a href="https://github.com/JdeRobot/RoboticsApplicationManager/pull/284">RoboticsApplicationManager #284</a></strong></li> <li><strong><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/742">RoboticsInfrastructure #742</a></strong></li> </ul> <p>Under review.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><summary type="html"><![CDATA[Making RAM manage two independent agent processes from a single exercise session — the foundation for Drone Cat-Mouse.]]></summary></entry><entry><title type="html">Gazebo City World &amp;amp; Aerostack2 Setup</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/gazebo-city-world-and-aerostack2-setup/" rel="alternate" type="text/html" title="Gazebo City World &amp;amp; Aerostack2 Setup"/><published>2026-06-01T06:30:00+00:00</published><updated>2026-06-01T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/gazebo-city-world-and-aerostack2-setup</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/gazebo-city-world-and-aerostack2-setup/"><![CDATA[<p>This week I worked on getting the simulation environment ready — built a custom city world, got drones flying in it, and wired up Aerostack2 as the flight stack.</p> <hr/> <h2 id="building-my-city">Building My City</h2> <p>The Cat-Mouse exercise needs a visually rich environment, not just a flat ground plane. So I modeled a full urban city in <strong>Blender</strong> and exported it as a <code class="language-plaintext highlighter-rouge">.glb</code> mesh for <strong>Gazebo Harmonic</strong>. I’m calling it <strong>my_city_v3</strong>.</p> <p>I added buildings of varying heights, a road network with highways and intersections, parking lots, a river cutting through the city, trees scattered around, schools, and even cars on the roads. Basically I wanted a miniature city that gives the drones something meaningful to fly around in.</p> <p>I loaded the mesh as a static model in the SDF world — static so Gazebo doesn’t waste physics cycles on it, but with a collision mesh so drones bounce off buildings if they crash.</p> <hr/> <h2 id="drones-spawned">Drones Spawned</h2> <p>I spawned two quadrotors in the city — one for the cat, one for the mouse. Both use the <strong>quadrotor_base</strong> model which I set up with four rotors, an IMU with realistic noise parameters, and a forward-facing camera (1280×720, 30 Hz, 90° FOV).</p> <p>I made the drones namespace-isolated — all cat topics go through <code class="language-plaintext highlighter-rouge">/drone_cat/*</code>, all mouse topics through <code class="language-plaintext highlighter-rouge">/drone_mouse/*</code>. Only <code class="language-plaintext highlighter-rouge">/clock</code> is shared so both drones see the same simulation time.</p> <hr/> <h2 id="aerostack2--why-i-went-with-it">Aerostack2 — Why I Went With It</h2> <p>With drones in the world, the next thing I needed was a flight stack. Raw velocity commands work for quick testing, but for a proper exercise with takeoff sequences, state estimation, and motion control, I needed something more structured.</p> <p>I went with <strong>Aerostack2</strong> — it’s a ROS 2 framework for autonomous multi-aerial-robot systems, developed at Universidad Politécnica de Madrid. It’s natively built on ROS 2, designed for multi-drone scenarios, and has clean modularity — I can swap any component without touching the rest.</p> <p>Why I picked it for the Cat-Mouse exercise:</p> <ul> <li><strong>Multi-drone native</strong> — built for N drones from the start, not retrofitted</li> <li><strong>Platform independent</strong> — same code works in sim and on real hardware</li> <li><strong>Fully modular</strong> — state estimator, motion controller, and platform layer are all swappable plugins</li> <li><strong>Clean Python API</strong> — students interact through simple calls like <code class="language-plaintext highlighter-rouge">takeoff()</code>, <code class="language-plaintext highlighter-rouge">get_position()</code>, <code class="language-plaintext highlighter-rouge">set_cmd_vel()</code> without touching raw ROS 2 topics</li> </ul> <p>I gave each drone its own independent AS2 stack: a <strong>platform layer</strong> that bridges to Gazebo, a <strong>PID motion controller</strong> that tracks velocity references, a <strong>state estimator</strong> that publishes localization from ground truth, and a <strong>behaviors layer</strong> that handles arming and takeoff/land sequences. Both stacks run in parallel, fully isolated by namespace.</p> <hr/> <h2 id="initial-setup-demo">Initial Setup Demo</h2> <p>Here’s a quick video of the initial Gazebo city world with the drones spawned and ready:</p> <div style="margin: 1.5rem 0;"> <video width="100%" controls="" style="border-radius: 8px;"> <source src="/gsoc2026-Anish_Kumar/assets/video/gazebo-city-initial-setup.webm" type="video/webm"/> </video> </div> <hr/> <h2 id="whats-next">What’s Next</h2> <p>The simulation substrate is ready — city world loaded, drones spawned, Aerostack2 configured. Next I’ll be working on the <strong>chase algorithms and logic</strong> — the mouse’s autonomous path generation and the cat’s pursuit controller.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="gazebo"/><category term="aerostack2"/><category term="ros2"/><category term="drone-simulation"/><summary type="html"><![CDATA[Built a custom city environment in Gazebo Harmonic, spawned drones, and set up Aerostack2 as the flight control framework for the Cat-Mouse exercise.]]></summary></entry><entry><title type="html">Integrating Multi-Process Support into RAM</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/ram-multiprocess-integration/" rel="alternate" type="text/html" title="Integrating Multi-Process Support into RAM"/><published>2026-05-25T06:30:00+00:00</published><updated>2026-05-25T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/ram-multiprocess-integration</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/ram-multiprocess-integration/"><![CDATA[<p>This week I integrated multi-process support into RAM and tested it end-to-end inside the actual Robotics Academy GUI. The goal: make RAM manage N processes instead of one — run them together, pause them together, kill them together.</p> <h2 id="so-i-built-anish-manager-first">So I built Anish Manager first</h2> <p>Before touching RAM, I built the PoC — Anish Manager — to answer one question cleanly:</p> <blockquote> <p>Can a single Python program manage N child processes the way RAM needs to, using just standard Linux signals?</p> </blockquote> <p>Two dummy processes (<code class="language-plaintext highlighter-rouge">anish.py</code> and <code class="language-plaintext highlighter-rouge">pedro.py</code>) printed a counter every second. The coordinator could run, pause, resume, and stop both simultaneously. No custom protocol. Just three OS signals:</p> <ul> <li><strong>SIGSTOP</strong> — freezes a process (stays in memory, gets no CPU)</li> <li><strong>SIGCONT</strong> — resumes it</li> <li><strong>SIGKILL</strong> — kills it</li> </ul> <p>SIGSTOP + SIGCONT are the important pair. You can SIGSTOP all processes, unpause Gazebo, then SIGCONT all of them — every process sees the simulator already running at the moment it starts ticking. Clean sync, no custom handshake.</p> <p>The mentor wanted this proved offline first, then folded into RAM. This week was the folding part.</p> <hr/> <h2 id="folding-it-into-ram">Folding it into RAM</h2> <p>RAM currently has one variable, <code class="language-plaintext highlighter-rouge">self.application_process</code>, holding a single subprocess. Everywhere — pause, resume, terminate, disconnect — it operates on that one handle.</p> <p>To support N processes, I flipped it to a dict:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">application_processes</span> <span class="o">=</span> <span class="p">{}</span>   <span class="c1"># name → Popen
</span></code></pre></div></div> <p>Six methods got updated:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">on_run_application</code></strong> — Popen the user code, store in dict. Added SIGSTOP → unpause sim → SIGCONT sync block. Wrapped in <code class="language-plaintext highlighter-rouge">try/finally</code> so no process gets left frozen if unpausing fails.</li> <li><strong><code class="language-plaintext highlighter-rouge">on_pause</code></strong> — iterate dict, SIGSTOP each process and its children, then pause sim.</li> <li><strong><code class="language-plaintext highlighter-rouge">on_resume</code></strong> — iterate dict, SIGCONT each, then unpause sim.</li> <li><strong><code class="language-plaintext highlighter-rouge">on_terminate_application</code> / <code class="language-plaintext highlighter-rouge">on_disconnect</code></strong> — call a new <code class="language-plaintext highlighter-rouge">_kill_all_applications()</code> helper that loops through and SIGKILLs everything.</li> <li><strong>SIGINT handler</strong> — also routes through the helper now.</li> </ul> <p>Once the dict exists, the rest is just for-loops. Adding a 2nd or 3rd process later is one dict insertion — sync, pause, resume, kill all already work on whatever is in there.</p> <hr/> <h2 id="does-it-actually-work">Does it actually work?</h2> <p>To verify all four operations work (not just in isolation), I added a temporary test hook inside <code class="language-plaintext highlighter-rouge">on_run_application</code>. After launching the editor code as <code class="language-plaintext highlighter-rouge">processA</code>, it also spawns <code class="language-plaintext highlighter-rouge">processB.py</code> — a standalone script that prints <code class="language-plaintext highlighter-rouge">pB: 0, pB: 1, ...</code> every second.</p> <p>Ran <code class="language-plaintext highlighter-rouge">follow_line</code> in the browser, wrote a <code class="language-plaintext highlighter-rouge">pA: n</code> loop in the editor, hit Run:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pA: 6
pB: 6
pA: 7
pB: 7
pA: 8
pB: 8
</code></pre></div></div> <p>Both counters climbing together. Pause — both froze. Resume — both continued from where they stopped. Stop — both dead, console silent.</p> <p>All four operations confirmed working across the full dict, not just one process.</p> <p>Here’s the demo:</p> <div style="margin: 1.5rem 0;"> <video width="100%" controls="" style="border-radius: 8px;"> <source src="/gsoc2026-Anish_Kumar/assets/video/ram-multiprocess-demo.webm" type="video/webm"/> </video> </div> <hr/> <h2 id="whats-next">What’s next</h2> <p>The test hook is hardcoded for now — RAM spawns <code class="language-plaintext highlighter-rouge">processB</code> on its own, not from any external input. The process management layer itself is done and verified. Further integration work is ongoing.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><summary type="html"><![CDATA[How a small PoC called Anish Manager proved the concept, and how that concept got folded into the real RoboticsApplicationManager codebase.]]></summary></entry><entry><title type="html">Community Bonding: Platform Architecture &amp;amp; Multi-Agent Proposal</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/community-bonding/" rel="alternate" type="text/html" title="Community Bonding: Platform Architecture &amp;amp; Multi-Agent Proposal"/><published>2026-05-18T06:30:00+00:00</published><updated>2026-05-18T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/community-bonding</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/community-bonding/"><![CDATA[<p>GSoC 2026 community bonding week — spent time deep diving into JdeRobot’s platform architecture before touching any exercise code.</p> <hr/> <h2 id="three-repos-one-platform">Three repos, one platform</h2> <p>JdeRobot splits into three clean layers.</p> <p><strong>RoboticsAcademy (RA)</strong> is the web interface — Django backend, React frontend, PostgreSQL.</p> <p><strong>RoboticsInfrastructure (RI)</strong> is the simulation substrate — Gazebo worlds, SDF models, ROS 2 launch files, drone packages.</p> <p><strong>RoboticsApplicationManager (RAM)</strong> is the conductor — a Python state machine on WebSocket port 7163 that drives the full exercise lifecycle from <code class="language-plaintext highlighter-rouge">Launch</code> to <code class="language-plaintext highlighter-rouge">Stop</code> to <code class="language-plaintext highlighter-rouge">Reset</code>.</p> <hr/> <h2 id="the-exercise-drone-cat-mouse-chase">The exercise: Drone Cat-Mouse Chase</h2> <p>Two drones in the same Gazebo world. Student programs the cat. Mouse follows a pre-programmed path. Cat has to catch it.</p> <p>The challenge is making two independent agents — one student-controlled, one pre-programmed — work together cleanly inside a platform that currently assumes a <strong>single agent per exercise</strong>.</p> <hr/> <h2 id="multi-agent-architecture-proposal">Multi-agent architecture proposal</h2> <p>Current RAM has one <code class="language-plaintext highlighter-rouge">application_process</code> — the student’s code. For cat-mouse, the mouse needs to run alongside the student’s cat code, starting and stopping together on every Play/Stop cycle.</p> <p><strong>Proposal:</strong> add a <code class="language-plaintext highlighter-rouge">companions</code> list to RAM. Companions are pre-programmed agent processes that start when the student clicks Run and stop when they click Stop. Mouse node becomes a companion. Config comes from the exercise universe entry — no RAM state machine changes needed, fully backward compatible with all existing exercises.</p> <p>This generalises cleanly to any future multi-agent exercise:</p> <table> <thead> <tr> <th>Exercise</th> <th>Student controls</th> <th>Companions</th> </tr> </thead> <tbody> <tr> <td>Drone Cat-Mouse</td> <td>cat drone</td> <td><code class="language-plaintext highlighter-rouge">mouse_node</code></td> </tr> <tr> <td>Formation Flying</td> <td>leader drone</td> <td><code class="language-plaintext highlighter-rouge">follower × N</code></td> </tr> <tr> <td>Multi-robot Warehouse</td> <td>planner</td> <td><code class="language-plaintext highlighter-rouge">executor × N</code></td> </tr> <tr> <td>Cooperative Search</td> <td>coordinator</td> <td><code class="language-plaintext highlighter-rouge">searcher × N</code></td> </tr> </tbody> </table> <p>Same pattern every time — one student agent, N pre-programmed peers. The companions mechanism handles the N peers uniformly regardless of robot type or domain.</p> <hr/> <h2 id="exercise-lifecycle-end-to-end">Exercise lifecycle end-to-end</h2> <h3 id="launch-click">Launch click</h3> <ul> <li>Gazebo loads → both <code class="language-plaintext highlighter-rouge">drone0</code> and <code class="language-plaintext highlighter-rouge">drone1</code> models spawn in world</li> <li>Both AS2 stacks start (platform + state estimator + motion controller for each)</li> <li>Both drones sitting on ground, armed, ready</li> </ul> <h3 id="run-click">Run click</h3> <ul> <li>RAM starts companions list → <code class="language-plaintext highlighter-rouge">mouse_node</code> starts as companion process</li> <li>RAM starts <code class="language-plaintext highlighter-rouge">academy.py</code> (student cat code)</li> <li><code class="language-plaintext highlighter-rouge">mouse_node</code>: takeoff <code class="language-plaintext highlighter-rouge">drone1</code> → reaches target height → begins pre-programmed path</li> <li><code class="language-plaintext highlighter-rouge">academy.py</code>: student cat code runs → takeoff <code class="language-plaintext highlighter-rouge">drone0</code> → starts chasing mouse position</li> </ul> <h3 id="chase">Chase</h3> <ul> <li><code class="language-plaintext highlighter-rouge">mouse_node</code> drives <code class="language-plaintext highlighter-rouge">drone1</code> via <code class="language-plaintext highlighter-rouge">/drone1/*</code> topics → circle / sin wave / figure-8 path</li> <li>Student cat code reads <code class="language-plaintext highlighter-rouge">/drone1/odometry</code> for mouse position → computes pursuit → drives <code class="language-plaintext highlighter-rouge">drone0</code> via <code class="language-plaintext highlighter-rouge">/drone0/*</code> topics</li> <li>Fully decoupled, namespace-isolated, no interference</li> </ul> <h3 id="stop-click">Stop click</h3> <ul> <li>RAM kills <code class="language-plaintext highlighter-rouge">academy.py</code> <strong>and</strong> all <code class="language-plaintext highlighter-rouge">companion_processes</code> together</li> <li>Both drones stop</li> <li><code class="language-plaintext highlighter-rouge">gz reset</code> → both snap to spawn positions</li> <li>Back to <code class="language-plaintext highlighter-rouge">tools_ready</code></li> </ul> <h3 id="play-again">Play again</h3> <ul> <li>RAM spawns fresh <code class="language-plaintext highlighter-rouge">companion_processes</code> → <code class="language-plaintext highlighter-rouge">mouse_node</code> restarts, <code class="language-plaintext highlighter-rouge">t=0</code>, path from beginning</li> <li>RAM spawns fresh <code class="language-plaintext highlighter-rouge">academy.py</code> → cat code restarts clean</li> <li>No stale timer, no stale state anywhere</li> </ul> <hr/> <h2 id="why-not-put-mouse_node-in-the-launcher">Why not put mouse_node in the launcher?</h2> <p>The old approach — mouse_node inside the Gazebo launcher — means RAM never gets a handle on it. It never gets killed on Stop, never restarts cleanly on Play.</p> <p>The companions list gives RAM <strong>full ownership of the lifecycle</strong>: killed and restarted on every Play/Stop cycle, same as the student’s code. No stale state. No manual cleanup. Works the same way for any future multi-agent exercise with any number of companion agents.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="community-bonding"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><summary type="html"><![CDATA[Deep dive into JdeRobot's three-repo platform architecture and a concrete proposal for multi-agent support in RAM.]]></summary></entry></feed>