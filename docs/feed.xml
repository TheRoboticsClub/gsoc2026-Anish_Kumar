<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/feed.xml" rel="self" type="application/atom+xml"/><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-12T18:28:49+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/feed.xml</id><title type="html">blank</title><subtitle>GSoC 2026 weekly blog — Drone Cat-Mouse Chase Exercise with JdeRobot Robotics Academy. </subtitle><entry><title type="html">GSoC 2026 Final Report — Drone Cat-Mouse Chase with Two Concurrent Drones</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/gsoc-final-report/" rel="alternate" type="text/html" title="GSoC 2026 Final Report — Drone Cat-Mouse Chase with Two Concurrent Drones"/><published>2026-08-12T06:30:00+00:00</published><updated>2026-08-12T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/gsoc-final-report</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/gsoc-final-report/"><![CDATA[<h2 id="about-me">About me</h2> <p>I’m Anish, a third-year student at IIT Mandi studying robotics and artificial intelligence. The area where software and physical systems meet — systems that must sense and navigate their surroundings, and the infrastructure that enables them to do so — is what most intrigues me. Open source is important to me, and I’m the type of person who would much prefer to spend a weekend troubleshooting a robot than practically anything else.</p> <hr/> <h2 id="about-the-project">About the project</h2> <p>Robotics Academy is a free platform for learning robotics. You write your program in the browser on one side of the screen, and a simulated robot does what you told it on the other side.</p> <p>It had one limit. Only one robot could be in the world at a time.</p> <blockquote> <p>So a whole class of exercises was impossible. Two robots chasing each other. Two arms working on the same table. A group of robots cleaning a house together. None of these could be built, because the platform could only ever start one program and drive one robot.</p> </blockquote> <p>My project was to remove that limit, and then build an exercise that proves it works.</p> <hr/> <h2 id="what-exists-now">What exists now</h2> <p>Robotics Academy can now run many robots in the same world. Each robot runs its own program, and they all run at the same time.</p> <p>To show it working, I built a new exercise called <strong>Drone Cat and Mouse</strong>. Two drones fly in a city. One drone runs away, and you write the code for the other one that has to catch it. There are three difficulty levels.</p> <hr/> <h2 id="the-work">The work</h2> <p>I will split this into four parts, because that is roughly how it happened.</p> <h3 id="1-running-more-than-one-program-at-a-time">1. Running more than one program at a time</h3> <p>The first problem was not the robots at all. It was that the part of the platform that runs your code, called the Robotics Application Manager, could only keep track of one running program.</p> <p>I did not want to touch the real thing straight away, so I first built a small separate program of my own to test the idea. It started a few programs, watched them, and stopped them. Once that worked I moved the same approach into the real manager.</p> <blockquote> <p>Where the manager used to hold one program, it now holds a list. Almost everything around it assumed there was only one, so a lot of smaller things had to be fixed along with it.</p> </blockquote> <p>Blog posts: <a href="/gsoc2026-Anish_Kumar/blog/2026/ram-multiprocess-integration/">Integrating multi-process support into RAM</a>, <a href="/gsoc2026-Anish_Kumar/blog/2026/multiprocess-code-execution/">Multi-process code execution</a></p> <hr/> <h3 id="2-from-one-robot-to-many">2. From one robot to many</h3> <p>Next was the simulation side. Even if you could run three programs, all three would end up talking to the same robot, because every robot was published on the same channels.</p> <p>The fix was to give every robot its own name, and to make the launch files take that name as an input instead of having it written inside them.</p> <p>The database also had to change. I proposed a many-to-many relationship between worlds and robots to the maintainers. The world used to say “put this robot here, this many times”, which cannot describe where each copy goes. Now it says “here are the robots, and here is where each one starts”. That is what lets one line in the database put five drones in five different places.</p> <blockquote> <p>After this, adding a second robot to a world stopped being a code change. It became a row in the database.</p> </blockquote> <p>I tested this with two F1 cars first, before any drone existed, because cars are simpler and if it worked for them it would work for anything else. Seeing two cars spawn in one world, each listening to its own program, was the point where I knew the project would work.</p> <p>Blog posts: <a href="/gsoc2026-Anish_Kumar/blog/2026/n-robots-support/">Moving from N users to N robots</a>, <a href="/gsoc2026-Anish_Kumar/blog/2026/n-robots-in-ram-testing-end-to-end/">N robots in RAM, tested end to end</a></p> <hr/> <h3 id="3-the-drone-cat-and-mouse-exercise">3. The Drone Cat and Mouse exercise</h3> <p>With the platform able to hold two robots, I built the exercise.</p> <p>The world is a city, which I modelled in Blender and then exported into Gazebo, and the drones fly using Aerostack2. One drone runs away on a fixed path. The other one is yours.</p> <p>Then I added three levels. Easy is a straight line and the escaping drone ignores you completely. Medium flies a lap and dodges when you get close. Hard uses height as well, jukes harder, and hides behind things. Each level has its own clock, and you score based on how fast you catch it.</p> <blockquote> <p>All three levels run the same escape program. Which level it flies is decided by the world you picked.</p> </blockquote> <p>Blog posts: <a href="/gsoc2026-Anish_Kumar/blog/2026/gazebo-city-world-and-aerostack2-setup/">Gazebo city world</a>, <a href="/gsoc2026-Anish_Kumar/blog/2026/drone-cat-mouse-integration/">Getting the exercise into Robotics Academy</a>, <a href="/gsoc2026-Anish_Kumar/blog/2026/two-drones-one-manager/">Two drones, one manager</a>, <a href="/gsoc2026-Anish_Kumar/blog/2026/chase-algorithms-and-evasive-mouse/">Chase algorithms and the hard level</a></p> <hr/> <h3 id="4-chasing-with-a-camera-without-ground-truth">4. Chasing with a camera, without ground truth</h3> <p>At first the chasing drone just read the other drone’s real position from the simulator. That works, but it teaches nothing, because there is no problem left to solve.</p> <p>So I took that away. The chasing drone now only has its camera. It has to find the other drone in the picture, turn until it is in the middle, and fly at it.</p> <blockquote> <p>This is where it got interesting, because a lot of things that seem obvious do not work. Turning towards where the target is now is always too late, because by the time you react it has already moved. You have to turn towards where it is going.</p> </blockquote> <p>On the hard level this matters a lot. The escaping drone takes sharp turns around a tree, and if you chase it blindly you go straight into the trunk and lose.</p> <p>Blog post: <a href="/gsoc2026-Anish_Kumar/blog/2026/from-ground-truth-to-the-camera/">From ground truth to the camera</a></p> <hr/> <h2 id="demo-video">Demo video</h2> <div style="position:relative;padding-bottom:56.25%;height:0;overflow:hidden;"> <iframe src="https://www.youtube.com/embed/Jj9ORzrbMdQ" style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" allowfullscreen=""></iframe> </div> <p>This is the whole project in one place: Robotics Academy could only run one robot before, and now it runs many, with the chase as the thing that proves it.</p> <p>In the video I show what the project was for, using the two robot exercise. The Drone Cat and Mouse chase is played on all three levels, with the cat drone chasing the mouse drone (2 robots) and the score at the end of each run.</p> <p>After that I show the same thing at a bigger size, because two robots was never the actual limit. There is a world with five drones flying together, and the existing Vacuum Cleaner exercise running with three robots in one house instead of one.</p> <hr/> <h2 id="pull-requests">Pull requests</h2> <table> <thead> <tr> <th>Date</th> <th>Repository</th> <th>PR</th> <th>What it did</th> </tr> </thead> <tbody> <tr> <td>24 Jun 2026</td> <td>RoboticsAcademy</td> <td><a href="https://github.com/JdeRobot/RoboticsAcademy/pull/3872">#3872</a></td> <td>Drone Cat and Mouse moved to ROS 2</td> </tr> <tr> <td>24 Jun 2026</td> <td>RoboticsInfrastructure</td> <td><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/742">#742</a></td> <td>The world, the robots and the launch files for the exercise</td> </tr> <tr> <td>23 Jul 2026</td> <td>RoboticsAcademy</td> <td><a href="https://github.com/JdeRobot/RoboticsAcademy/pull/3938">#3938</a></td> <td>Two drones and two programs in one exercise</td> </tr> <tr> <td>24 Jul 2026</td> <td>RoboticsInfrastructure</td> <td><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/781">#781</a></td> <td>Colour option for the drone</td> </tr> <tr> <td>3 Aug 2026</td> <td>RoboticsAcademy</td> <td><a href="https://github.com/JdeRobot/RoboticsAcademy/pull/3954">#3954</a></td> <td>Adding the difficulty levels</td> </tr> <tr> <td>3 Aug 2026</td> <td>RoboticsInfrastructure</td> <td><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/787">#787</a></td> <td>A launch file for each of the three levels</td> </tr> <tr> <td>4 Aug 2026</td> <td>RoboticsInfrastructure</td> <td><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/789">#789</a></td> <td>The chasing drone reuses the shared drone definition</td> </tr> </tbody> </table> <h3 id="robotics-application-manager">Robotics Application Manager</h3> <p>The multi-program support in the manager repository came from my work, but it was not merged from my own pull request. After the review, the maintainer took part of it and opened <a href="https://github.com/JdeRobot/RoboticsApplicationManager/pull/298">#298</a> himself, and that is the one that got merged. So what is running in the manager today is based on my initial contribution.</p> <hr/> <h2 id="future-work">Future work</h2> <p>Only Drone Cat and Mouse uses more than one robot so far. The platform can hold many now, so the interesting part is what gets built on top of it. Some of these already have everything they need in Robotics Academy:</p> <ul> <li><strong>Pick and Place</strong> — two arms building the same pallet, working either side of it without getting in each other’s way</li> <li><strong>Warehouse</strong> — a handful of robots working the same aisles, deciding who goes first and who waits</li> <li><strong>Follow Line</strong> — four cars on one circuit, which turns a lap time into a race</li> <li><strong>Rescue People</strong> — two drones splitting the search between them instead of one covering everything</li> <li><strong>Swarms and formation flying</strong> — many drones holding a shape while they move, which I already tried with six drones in one world</li> </ul> <blockquote> <p>None of these need new platform work. They only need someone to write them, and that is the part I am most happy about — the next exercise with more than one robot can come from anyone.</p> </blockquote> <hr/> <h2 id="thank-you">Thank you</h2> <p>I could not have asked for better mentors than <strong>Jose Maria Canas</strong> and <strong>Pedro Arias-Perez</strong>. They were really very supportive throughout. The way they pick up a concept and explain it starting from the simplest form, and then take it slowly to the harder parts, is the thing I learned the most from. I made mistakes many times during this project, and every time they explained it patiently instead of just telling me it was wrong.</p> <p>Thank you to JdeRobot for taking me in, and to Google Summer of Code.</p> <p>I am not finished with this. I will keep maintaining what I built and help other developers who want to use it.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="multi-agent"/><category term="robotics-academy"/><summary type="html"><![CDATA[Drone Cat-Mouse Chase with Two Concurrent Drones · Multi-Robot Support for Robotics Academy]]></summary></entry><entry><title type="html">Chase Algorithms, an Evasive Mouse, and the Hard Level</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/chase-algorithms-and-evasive-mouse/" rel="alternate" type="text/html" title="Chase Algorithms, an Evasive Mouse, and the Hard Level"/><published>2026-08-04T06:30:00+00:00</published><updated>2026-08-04T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/chase-algorithms-and-evasive-mouse</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/chase-algorithms-and-evasive-mouse/"><![CDATA[<p>This week was the part of the project I’ve been looking forward to the most — the algorithms. Two PRs landed, all three difficulty levels came together end to end, and the mouse stopped being a moving target and started being an actual opponent.</p> <hr/> <h2 id="two-merges">Two merges</h2> <p><strong><a href="https://github.com/JdeRobot/RoboticsAcademy/pull/3954">RoboticsAcademy #3954</a></strong> — exercise assets, mouse code, and student template for the cat-and-mouse exercise in RoboticsAcademy.</p> <p><strong><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/787">RoboticsInfrastructure #787</a></strong> — three separate launchers for the exercise, one per difficulty level.</p> <hr/> <h2 id="three-levels-one-mouse">Three levels, one mouse</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/gsoc2026-Anish_Kumar/assets/img/levels_image-480.webp 480w,/gsoc2026-Anish_Kumar/assets/img/levels_image-800.webp 800w,/gsoc2026-Anish_Kumar/assets/img/levels_image-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/gsoc2026-Anish_Kumar/assets/img/levels_image.png" class="img-fluid rounded" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Easy, Medium, and Hard — same mouse code, three personalities.</figcaption> </figure> <p>The exercise ships three worlds — Easy, Medium, and Hard — and a single mouse program that handles all three. Which mode it flies is determined by which world the student launched. The world publishes that over a ROS topic when it starts up, and the mouse reads it and picks its behaviour accordingly.</p> <table> <thead> <tr> <th> </th> <th>Speed</th> <th>Reacts to cat</th> <th>Jukes</th> <th>Timer</th> </tr> </thead> <tbody> <tr> <td>Easy</td> <td>1.8 m/s</td> <td>No</td> <td>No</td> <td>30 s</td> </tr> <tr> <td>Medium</td> <td>2.2 m/s</td> <td>Yes</td> <td>Yes (every 2.0 s)</td> <td>60 s</td> </tr> <tr> <td>Hard</td> <td>2.2 m/s</td> <td>Yes, harder</td> <td>Yes (every 1.6 s)</td> <td>90 s</td> </tr> </tbody> </table> <p>Easy ignores the cat completely. Hard uses altitude as well as the ground plane. Same code, three different personalities set by a config.</p> <hr/> <h2 id="how-the-cat-tracks-the-mouse">How the cat tracks the mouse</h2> <p>The cat only gets a 320×240 camera at around 20 frames per second. Everything below is about turning that image into a speed and direction.</p> <h3 id="spotting-the-mouse">Spotting the mouse</h3> <p>The detector never changed across any of the algorithm iterations — everything else did.</p> <p>Convert the frame to HSV colour space, then look for magenta. Magenta wraps around the ends of the hue range, so you need two overlapping masks combined. Apply a small morphological open to remove noise, then a close to fill in the gaps the spinning rotors leave in the blob. Take the biggest contour. That’s the whole detector — about 15 lines.</p> <p>The blob’s <strong>width</strong> tells you how far away the mouse is. The quadrotor is 0.47 m across and the camera covers 59° over 320 pixels:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>width_px ≈ 0.47 × 283 / distance
  33 px  -&gt;  4.0 m   (comfortable following distance)
  74 px  -&gt;  1.8 m   (catch radius)
</code></pre></div></div> <p>The city has red brick and red cars everywhere. Magenta doesn’t appear anywhere in the world, which is exactly why it was chosen.</p> <h3 id="the-simplest-tracker--works-on-easy-breaks-on-medium">The simplest tracker — works on Easy, breaks on Medium</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">yaw</span>  <span class="o">=</span> <span class="o">-</span><span class="n">K_YAW</span> <span class="o">*</span> <span class="p">(</span><span class="n">blob_x</span> <span class="o">-</span> <span class="n">cx</span><span class="p">)</span>
<span class="n">vz</span>   <span class="o">=</span> <span class="o">-</span><span class="n">K_VZ</span>  <span class="o">*</span> <span class="p">(</span><span class="n">blob_y</span> <span class="o">-</span> <span class="n">cy</span><span class="p">)</span>
<span class="n">vx</span>   <span class="o">=</span> <span class="n">CRUISE</span>
</code></pre></div></div> <p>Three lines. If the mouse is left of centre, turn left. If it’s above centre, climb. Always move forward. That’s enough to catch Easy, which just flies a steady lap without doing anything tricky. Medium is harder — the mouse is turning, and by the time the cat reacts, it’s already behind. That failure is intentional. Each level is designed so the simplest thing that worked on the previous one isn’t enough.</p> <h3 id="smoothing-the-signal">Smoothing the signal</h3> <p>The raw blob centre shimmers frame to frame — the bounding box breathes a little as the rotors spin, and the detector picks that up. A controller that reacts to every tiny flicker makes the drone jitter.</p> <p>I tried a Kalman filter first — a standard way to smooth noisy readings while also estimating how fast the target is moving. It didn’t work well here. When the mouse is far away the blob is tiny and its centre is unreliable; when it’s close the blob is large and very precise. A single filter tuned for one case behaves badly in the other. Worse, when the mouse makes a sudden dodge, the filter’s built-in “things don’t change abruptly” assumption works against you — it smooths right through the moment you most needed to see.</p> <p>What I used instead is much simpler:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">smooth_u</span> <span class="o">=</span> <span class="n">smooth_u</span> <span class="o">+</span> <span class="mf">0.35</span> <span class="o">*</span> <span class="p">(</span><span class="n">u</span> <span class="o">-</span> <span class="n">smooth_u</span><span class="p">)</span>
<span class="n">drift</span>    <span class="o">=</span> <span class="mf">0.8</span> <span class="o">*</span> <span class="n">drift</span> <span class="o">+</span> <span class="mf">0.2</span> <span class="o">*</span> <span class="p">(</span><span class="n">smooth_u</span> <span class="o">-</span> <span class="n">prev_u</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">smooth_u</code> is just an exponential moving average of the blob’s horizontal position — each frame it moves 35% of the way toward the new reading. <code class="language-plaintext highlighter-rouge">drift</code> tracks how fast <code class="language-plaintext highlighter-rouge">smooth_u</code> is changing, which is essentially an estimate of how fast the mouse is moving across the frame. Two numbers, no setup, and it handles the tiny-blob case fine because it doesn’t try to be confident about anything.</p> <h3 id="turning-ahead-of-where-the-mouse-is--works-on-medium">Turning ahead of where the mouse is — works on Medium</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">yaw</span> <span class="o">=</span> <span class="nf">clamp</span><span class="p">(</span><span class="o">-</span><span class="n">K_YAW</span> <span class="o">*</span> <span class="p">(</span><span class="n">smooth_u</span> <span class="o">-</span> <span class="n">cx</span><span class="p">)</span> <span class="o">-</span> <span class="n">K_LEAD</span> <span class="o">*</span> <span class="n">drift</span><span class="p">,</span> <span class="n">MAX_YAW</span><span class="p">)</span>
</code></pre></div></div> <p>Instead of turning based only on where the blob is right now, I also factor in <code class="language-plaintext highlighter-rouge">drift</code> — which direction the blob is already moving. So the cat starts turning <em>before</em> the mouse reaches the edge of frame, not after it’s already gone. That’s enough to stay on Medium through its loops. Hard is a different problem because now the mouse is actively trying to throw the cat off.</p> <h3 id="when-the-mouse-disappears-from-frame">When the mouse disappears from frame</h3> <p>When the blob leaves the frame, the obvious move is to turn toward where it was last seen. But that’s always too late — the mouse kept moving while the cat was figuring out it had gone. So instead, I project the last known position forward a few frames:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">exit_u</span> <span class="o">=</span> <span class="n">last_u</span> <span class="o">+</span> <span class="n">last_drift</span> <span class="o">*</span> <span class="mi">8</span>   <span class="c1"># where it probably is now
</span><span class="n">side</span>   <span class="o">=</span> <span class="o">+</span><span class="mi">1</span> <span class="k">if</span> <span class="n">exit_u</span> <span class="o">&lt;</span> <span class="n">cx</span> <span class="k">else</span> <span class="o">-</span><span class="mi">1</span>
<span class="n">yaw</span>    <span class="o">=</span> <span class="n">side</span> <span class="o">*</span> <span class="p">(</span><span class="n">YAW_MIN</span> <span class="o">+</span> <span class="n">urgency</span> <span class="o">*</span> <span class="p">(</span><span class="n">YAW_MAX</span> <span class="o">-</span> <span class="n">YAW_MIN</span><span class="p">))</span>
<span class="n">vx</span>     <span class="o">=</span> <span class="n">CRUISE</span> <span class="o">*</span> <span class="mf">0.4</span>              <span class="c1"># slow down — flying blind
</span></code></pre></div></div> <p>If the mouse slid off the right edge fast, turn right hard and slow down. If it drifted off the centre top, turn gently and keep moving. The idea is to sweep the target back into frame rather than spin in place hoping it comes back.</p> <h3 id="slowing-down-for-turns--avoiding-trees-without-detecting-them">Slowing down for turns — avoiding trees without detecting them</h3> <p>This is the idea I enjoyed most this week. The Hard course sends the mouse wrapping around a tree. If the cat just follows the blob, it flies straight into the trunk. The cat has no depth sensor and no map — but it doesn’t need either:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">turning</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="nf">abs</span><span class="p">(</span><span class="n">drift</span><span class="p">)</span> <span class="o">/</span> <span class="n">TURN_DRIFT</span><span class="p">)</span>
<span class="n">vx</span>      <span class="o">=</span> <span class="n">vx</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">TURN_BRAKE</span><span class="p">)</span> <span class="o">*</span> <span class="n">turning</span><span class="p">)</span>
</code></pre></div></div> <p>If the blob is sliding fast across the frame, the mouse is turning sharply. And if the mouse is turning sharply, there’s probably something it’s turning around. So the cat backs off the throttle proportionally — fast blob drift means slow down. The tree is never detected. The collision just stops happening.</p> <h3 id="a-bug-that-was-keeping-the-cat-from-ever-catching-anything">A bug that was keeping the cat from ever catching anything</h3> <p>I spotted this late. The speed law was:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vx</span> <span class="o">=</span> <span class="n">CRUISE</span> <span class="o">+</span> <span class="n">K_VX</span> <span class="o">*</span> <span class="p">(</span><span class="n">TARGET_WIDTH</span> <span class="o">-</span> <span class="n">width</span><span class="p">)</span>
</code></pre></div></div> <p>The idea is to speed up when far and slow down when close. But <code class="language-plaintext highlighter-rouge">TARGET_WIDTH = 33 px</code> corresponds to 4 m, so this term goes negative inside 4 m. At the actual catch distance of 1.8 m the cat was commanding <em>less</em> speed than the mouse. It had been hovering at a comfortable 4 m distance and never actually going in. The fix:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vx</span> <span class="o">=</span> <span class="n">CRUISE</span> <span class="o">+</span> <span class="n">K_VX</span> <span class="o">*</span> <span class="nf">max</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">TARGET_WIDTH</span> <span class="o">-</span> <span class="n">width</span><span class="p">)</span>  <span class="c1"># only ever speeds up
</span>
<span class="k">if</span> <span class="n">width</span> <span class="o">&gt;</span> <span class="n">LUNGE_WIDTH</span><span class="p">:</span>   <span class="c1"># inside ~6 m
</span>    <span class="n">vx</span>  <span class="o">=</span> <span class="n">MAX_VX</span>
    <span class="n">yaw</span> <span class="o">=</span> <span class="n">yaw</span> <span class="o">*</span> <span class="n">LUNGE_YAW</span>
</code></pre></div></div> <p>Inside six metres, slow and cautious is what loses it.</p> <hr/> <h2 id="how-the-mouse-fights-back">How the mouse fights back</h2> <p>The mouse isn’t a fixed recording. It reads the cat’s live position from a ROS topic every loop and decides what to do about it. The two drones never communicate directly — each only sees the other through the simulator, which is the same limitation the cat faces with its camera.</p> <h3 id="the-juke">The juke</h3> <p>When the cat gets close enough, the mouse starts blending its normal lap-following with a sideways push — perpendicular to the line between the two drones. It flips which side it pushes toward on a timer, so the cat can’t just mirror it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">push</span> <span class="o">=</span> <span class="p">(</span><span class="n">THREAT_RADIUS</span> <span class="o">-</span> <span class="n">d</span><span class="p">)</span> <span class="o">/</span> <span class="n">THREAT_RADIUS</span>
<span class="n">away</span> <span class="o">=</span> <span class="nf">normalise</span><span class="p">(</span><span class="n">position</span> <span class="o">-</span> <span class="n">cat</span><span class="p">)</span>
<span class="n">side</span> <span class="o">=</span> <span class="nf">perpendicular</span><span class="p">(</span><span class="n">away</span><span class="p">)</span> <span class="o">*</span> <span class="n">juke_side</span>

<span class="n">want</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">push</span><span class="p">)</span> <span class="o">*</span> <span class="n">course_vel</span> <span class="o">+</span> <span class="n">push</span> <span class="o">*</span> <span class="p">(</span><span class="n">side</span> <span class="o">+</span> <span class="mf">0.4</span> <span class="o">*</span> <span class="n">away</span><span class="p">)</span> <span class="o">*</span> <span class="n">DODGE_GAIN</span>
</code></pre></div></div> <h3 id="fixing-the-lap">Fixing the lap</h3> <p>Originally the mouse switched to the next gate when it got close enough by distance. At speed it would overshoot, find itself outside the gate radius, and turn back — flying the whole lap as a series of U-turns.</p> <p>One extra check fixed it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">behind</span> <span class="o">=</span> <span class="nf">dot</span><span class="p">(</span><span class="n">gate</span> <span class="o">-</span> <span class="n">position</span><span class="p">,</span> <span class="n">gate</span> <span class="o">-</span> <span class="n">prev_gate</span><span class="p">)</span> <span class="o">&lt;</span> <span class="mi">0</span>
<span class="k">if</span> <span class="nf">dist</span><span class="p">(</span><span class="n">position</span><span class="p">,</span> <span class="n">gate</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">GATE_RADIUS</span> <span class="ow">or</span> <span class="n">behind</span><span class="p">:</span>
    <span class="n">advance</span> <span class="n">to</span> <span class="nb">next</span> <span class="n">gate</span>
</code></pre></div></div> <p>If the gate is now behind us along the direction we came from, move on.</p> <hr/> <h2 id="hard-level">Hard level</h2> <video width="100%" controls=""> <source src="/gsoc2026-Anish_Kumar/assets/video/hard.webm" type="video/webm"/> </video> <hr/> <h2 id="how-the-courses-were-designed">How the courses were designed</h2> <p>The gate positions aren’t guessed. The city is a 7 MB 3D model file, and I wrote a script to read the geometry out of it, convert the coordinates into Gazebo’s system, and compute a clearance function — minimum distance from any point to the nearest obstacle. Checking whether a proposed leg is flyable becomes sampling that function every 0.3 m along the path.</p> <p>That caught a corridor that looked clear on paper but had a 2.9 m gap in the middle of it. It’s also how the Hard course’s tree wrap exists at all — there’s exactly one tall pylon in the whole city, 11.7 m at roughly (25.5, −1.2), and the Hard course was designed specifically around it.</p> <hr/> <h2 id="tuning-notes">Tuning notes</h2> <p>A few things that weren’t obvious until something broke:</p> <p><strong>Running both drones at 4.6 m/s was too fast to track.</strong> The mouse crossed 16 pixels per frame sideways during a juke and the detector lost it every time. Halving both speeds made the chase actually winnable.</p> <p><strong>A low vertical speed limit meant the cat never followed the mouse upward.</strong> It would just hover underneath and watch the mouse climb away. Both a higher vertical speed and tracking the vertical blob drift were needed together — one without the other didn’t help.</p> <hr/> <h2 id="next">Next</h2> <p>Testing all three levels against the tuned reference solutions, then the scoring model. The mouse is capable enough now that the question has shifted from “can the cat catch it” to “how long should it take, and what does that mean for a score.”</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="aerostack2"/><category term="computer-vision"/><category term="multi-agent"/><summary type="html"><![CDATA[Three difficulty levels, five tracking ideas, one evasive drone that actually fights back, and the tuning decisions behind all of it.]]></summary></entry><entry><title type="html">From Ground Truth to the Camera</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/from-ground-truth-to-the-camera/" rel="alternate" type="text/html" title="From Ground Truth to the Camera"/><published>2026-07-28T06:30:00+00:00</published><updated>2026-07-28T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/from-ground-truth-to-the-camera</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/from-ground-truth-to-the-camera/"><![CDATA[<p>The first working chase used <code class="language-plaintext highlighter-rouge">HAL.get_mouse_position()</code> — the mouse’s real coordinates, straight from the simulator. Subtract, normalise, fly toward it. It caught the mouse every time and it was a terrible exercise: there’s no perception in it, just a subtraction.</p> <p>This week I swapped the ground truth out for what the cat’s own cameras can actually see.</p> <hr/> <h2 id="why-the-easy-approaches-kept-breaking">Why the easy approaches kept breaking</h2> <p>The first thing I tried was lead prediction — predict where the mouse will be from its current velocity and aim there instead of at where it is. That fixed overshooting on straight legs. Then the mouse flew a figure-eight and the prediction aimed at the wrong side of the loop on every single pass, because a constant-velocity guess is exactly wrong when the target is always turning.</p> <p>The real issue wasn’t the algorithm, it was what I was feeding it. A real drone doesn’t get handed the target’s GPS coordinates. An exercise that does that isn’t teaching anything useful. So I changed the rule: the only input the cat gets is what its own cameras can see.</p> <p>The mouse is magenta. The cat is orange. The cat never accidentally detects itself.</p> <hr/> <h2 id="detecting-the-mouse">Detecting the mouse</h2> <p>Convert the frame to HSV and threshold on hue. Magenta sits at both ends of the 0–180 hue range, so you need two masks OR’d together to catch it reliably. Apply an open-then-close to kill noise, find the biggest contour, read off its centroid and bounding box. That’s the whole detector — maybe 15 lines.</p> <hr/> <h2 id="getting-distance-from-a-regular-camera">Getting distance from a regular camera</h2> <p>The frontal camera has no depth sensor, so I used blob width as a distance proxy. If you know the real-world width of the object and the focal length of the camera in pixels, the math is straightforward:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>distance ≈ (real_width × focal_px) / blob_width_px
</code></pre></div></div> <p>At 320×240 with a 59° horizontal FOV the focal length comes out around 283 px. Good enough to maintain a comfortable following distance without either crashing into the mouse or losing it.</p> <hr/> <h2 id="turning-image-error-into-velocity">Turning image error into velocity</h2> <p>Since the camera faces forward on the drone, the error you read off the image maps directly onto what direction to fly — the axes basically decouple:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>horizontal blob offset  -&gt;  yaw rate    (turn left/right to centre it)
vertical blob offset    -&gt;  vz          (climb or descend)
blob width              -&gt;  vx          (move closer or back off)
</code></pre></div></div> <p>Three separate proportional controllers, each reading one thing from the same blob. None of them needs to know where the mouse actually is in the world.</p> <hr/> <h2 id="when-the-mouse-disappears">When the mouse disappears</h2> <p>The tracking part is the easy bit. The hard part is what happens when the blob is gone — mouse went behind a tree, or flew out of frame on a tight turn. If you just freeze and hold the last position you’ll drift off and lose it. What works better is to keep yawing in the direction the mouse was moving when you last saw it, so it sweeps back into view instead of you flying in the wrong direction.</p> <hr/> <h2 id="trees-were-a-bigger-problem-than-expected">Trees were a bigger problem than expected</h2> <p>The early failures looked like altitude bugs — the cat climbing or descending at the wrong moment — and they weren’t. It was hitting tree canopy. I went through the city model, found where the trees actually are, and redesigned the mouse courses so every leg has clearance. Not just at the waypoints — I sampled the whole path in between to make sure nothing was sticking up in the middle of a leg.</p> <hr/> <h2 id="three-courses-three-different-algorithms">Three courses, three different algorithms</h2> <p>This is the part I’m actually happy with.</p> <p><strong>Patrol</strong> — the mouse flies a slow, predictable rectangle. Reacting to the current frame is enough. Lead prediction is slightly smoother but not required.</p> <p><strong>Figure-eight</strong> — the mouse loops continuously and reverses direction twice each lap. Lead prediction helps on the straight sections and aims at the wrong half of the loop on every turn. What you actually need is to track the turn rate, not just the velocity.</p> <p><strong>Evasive</strong> — the mouse watches where the cat is and actively runs the other way. Pure reaction can never close the gap. You have to predict where it’s going to escape to and get there first.</p> <p><code class="language-plaintext highlighter-rouge">HAL.get_mouse_position()</code> is still in the API — not for chasing, but so students can sanity-check their visual tracker against the real answer while debugging. Once it’s working, you’re not supposed to need it.</p> <p>The progression is intentional: each course breaks the simplest thing that worked on the previous one, and the fix is always the next idea up the stack.</p> <hr/> <h2 id="two-merges-this-week">Two merges this week</h2> <p><strong><a href="https://github.com/JdeRobot/RoboticsAcademy/pull/3938">RoboticsAcademy #3938</a></strong> — the frontend now scans an exercise’s folders for any directory with its own entrypoint and sends a list, instead of assuming there’s always just one <code class="language-plaintext highlighter-rouge">academy.py</code>.</p> <p><strong><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/781">RoboticsInfrastructure #781</a></strong> — a <code class="language-plaintext highlighter-rouge">color</code> argument on the quadrotor model.</p> <hr/> <h2 id="getting-the-drones-different-colours">Getting the drones different colours</h2> <p>The cat and mouse need to look different from a 320×240 feed, so I added colour as a parameter on the drone model. The first attempt just defaulted to blue — which repainted every drone in every existing exercise, since they all share the same model file.</p> <p>The fix is an empty default with a conditional:</p> <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;xacro:if</span> <span class="na">value=</span><span class="s">"${color != ''}"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;material</span> <span class="na">name=</span><span class="s">"${color}"</span><span class="nt">/&gt;</span>
<span class="nt">&lt;/xacro:if&gt;</span>
</code></pre></div></div> <p>No colour argument, no override, the mesh keeps its original texture. There are three places that all need the same empty default — the xacro arg, the macro parameter, and the launch argument — and if you miss any one of them, something quietly gets repainted blue. <code class="language-plaintext highlighter-rouge">scale</code> works the same way.</p> <p>Neither gets parsed by RAM. Both just ride along in <code class="language-plaintext highlighter-rouge">extra_config</code> and get passed to <code class="language-plaintext highlighter-rouge">ros2 launch</code> as-is.</p> <hr/> <h2 id="three-worlds-in-the-database">Three worlds in the database</h2> <p>Cat-and-mouse now has three universes, all using the same city but with a different mouse behaviour each:</p> <ul> <li><strong>patrol</strong> — steady speed, fixed lap</li> <li><strong>figure_eight</strong> — constant turning, reverses twice per lap</li> <li><strong>evasive</strong> — actively runs from the cat</li> </ul> <p>Same three courses as above, now each its own selectable world. Which one launches is determined by the launch file, not the exercise code — same pattern the F1 exercises use for different circuits. RAM doesn’t know or care which one is running.</p> <hr/> <h2 id="ram-got-smaller">RAM got smaller</h2> <p>I’d built a small class to hold the running agent processes, keyed by name, so individual ones could be looked up and controlled. While rewriting it I noticed nothing ever actually looked one up individually. Every operation was “do this to all of them” — kill all, pause all, resume all. A dictionary you always iterate over completely is just a list with more code.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">application_processes</span> <span class="o">=</span> <span class="p">[]</span>
</code></pre></div></div> <p>Same result, one line.</p> <p>A real bug turned up too: if an exercise had both a C++ and a Python entrypoint, only the first one ever ran. The old code looked at <code class="language-plaintext highlighter-rouge">entrypoints[0]</code>, decided what kind it was, and returned. Fixing it meant changing <code class="language-plaintext highlighter-rouge">return</code> to <code class="language-plaintext highlighter-rouge">continue</code> so each entrypoint goes through its own dispatch. I also tried to be clever about <code class="language-plaintext highlighter-rouge">colcon</code> — restructured the build step so two C++ entrypoints would only trigger one build. Colcon is incremental, so the second call is essentially a no-op anyway, and the restructuring touched control flow that other exercises rely on. Reverted. The “duplicate” build stays.</p> <hr/> <h2 id="a-guard-that-stopped-working-in-july">A guard that stopped working in July</h2> <p>Something that cost me most of a day. There’s a line in <code class="language-plaintext highlighter-rouge">manager.py</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">robot_cfg</span><span class="p">[</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">]</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
</code></pre></div></div> <p>That check hasn’t been reachable since July 14. RoboticsAcademy used to send a placeholder config with <code class="language-plaintext highlighter-rouge">type: None</code> to signal “this world has no robot”. On July 14 it switched to just sending an empty list instead. RAM’s side never got updated, so the guard is there protecting against a value that nobody sends anymore.</p> <p>Reading only the RAM codebase there’s no way to know this — it looks like a normal active check.</p> <h2 id="the-camera-chase-in-action">The camera chase in action</h2> <video width="100%" controls=""> <source src="/gsoc2026-Anish_Kumar/assets/video/vision.webm" type="video/webm"/> </video> <hr/> <h2 id="the-bigger-picture-ram">The bigger picture: RAM</h2> <p>Getting the visual tracking to work is satisfying, but the RAM side is really where the project sits. Everything in this post — the multi-world setup, the colour arguments, the N-entrypoint dispatch — only makes sense because RAM now treats robots and agents as lists, not singletons. The camera exercise runs on top of the same infrastructure that lets any exercise define any number of robots and agents without touching the core. That’s the part I’m working through in review right now, and getting it merged cleanly is the main thing left.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="aerostack2"/><category term="computer-vision"/><category term="ibvs"/><summary type="html"><![CDATA[Camera-based chase, two merges, paintable drones, three worlds, and RAM getting smaller.]]></summary></entry><entry><title type="html">Two Drones, One Manager, and a Lot of Things That Were Only Pretending to Work</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/two-drones-one-manager/" rel="alternate" type="text/html" title="Two Drones, One Manager, and a Lot of Things That Were Only Pretending to Work"/><published>2026-07-22T06:30:00+00:00</published><updated>2026-07-22T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/two-drones-one-manager</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/two-drones-one-manager/"><![CDATA[<p>This week was less about writing new features and more about finding out which parts of the pipeline were quietly held together by assumptions. By the end, two drones are flying from two independent agent processes, and the path that gets them there doesn’t care whether it’s two or ten.</p> <hr/> <h2 id="we-launched-cat-and-mouse-and-got-three-drones">We launched cat-and-mouse and got three drones</h2> <p>Two of them sitting inside each other.</p> <p>The world SDF baked into the container image still had <code class="language-plaintext highlighter-rouge">cat_mouse_drone0</code> and <code class="language-plaintext highlighter-rouge">cat_mouse_drone1</code> as <code class="language-plaintext highlighter-rouge">&lt;include&gt;</code> entries, while the database was also asking for two quadrotors — so we ended up with four total.</p> <hr/> <h2 id="the-review-that-actually-made-the-design-better">The review that actually made the design better</h2> <p>I’d put the multi-robot logic in <code class="language-plaintext highlighter-rouge">manager.py</code>: uniquify the entity names, start every launch, then one polling loop waiting for all the entities to appear in Gazebo. It worked. Javier’s review came back with three comments, and each one was right.</p> <p><strong>“The main file of the RAM is sim agnostic. These imports must not be here.”</strong></p> <p>I had <code class="language-plaintext highlighter-rouge">from gz.transport13 import Node</code> at the top of the manager. The manager orchestrates; it isn’t supposed to know Gazebo exists. That import quietly made the whole state machine Gazebo-only.</p> <p><strong>“Inside of launcher_robots not here.”</strong></p> <p>The waiting and the renaming belong to the layer that owns robots, not to the orchestrator calling it.</p> <p><strong>“Use entrypoint to add the processB stuff. This should just be treated as another entrypoint.”</strong></p> <p>This was the big one. I’d been detecting a <code class="language-plaintext highlighter-rouge">processB/</code> folder by probing the filesystem — one hardcoded path, capped at exactly two agents.</p> <p>I moved the Gazebo polling into <code class="language-plaintext highlighter-rouge">launcher_robot_ros2_api.py</code> and gave each robot its own <code class="language-plaintext highlighter-rouge">wait()</code>. He came back again: one loop per robot means N sequential loops, which quietly undoes the parallelism the whole design was built for. The final shape:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>manager              -&gt; LauncherRobot.wait_for(launchers)   # one call, no gz
LauncherRobot        -&gt; gathers every entity, delegates once
LauncherRobotRos2Api -&gt; single poll loop ticking off the list
</code></pre></div></div> <p>Start every launch, then wait for all of them together. Spawn time is <code class="language-plaintext highlighter-rouge">max(one robot)</code> instead of <code class="language-plaintext highlighter-rouge">sum(all robots)</code>, and the manager never imports a simulator.</p> <hr/> <h2 id="the-namespace-that-wasnt-where-i-thought-it-was">The namespace that wasn’t where I thought it was</h2> <p>Two identical quadrotors arrive from the database as two identical configs. The entity is a top-level field, so uniquifying it is straightforward: <code class="language-plaintext highlighter-rouge">drone</code>, <code class="language-plaintext highlighter-rouge">drone_1</code>. I shipped that and both drones spawned fine.</p> <p>Then I looked at the ROS topics. Both were publishing to <code class="language-plaintext highlighter-rouge">/model/drone/cmd_vel</code>.</p> <p>The namespace isn’t a field — it lives buried inside a string:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>extra_config: "sensor:=camera namespace:=drone"
</code></pre></div></div> <p>My uniquify loop was looking for a <code class="language-plaintext highlighter-rouge">namespace</code> key, found nothing, and silently did nothing every single time. The Gazebo models were distinct, but the ROS graph was a collision. Now it pulls the value out with a regex and writes it back, with entity and namespace uniquified independently — a robot can have one and not the other, and a clash in one doesn’t imply a clash in the other.</p> <hr/> <h2 id="the-schema-moved-under-me">The schema moved under me</h2> <p>Mid-week, upstream replaced <code class="language-plaintext highlighter-rouge">instances</code> (a count) with <code class="language-plaintext highlighter-rouge">poses</code> (a list) on the world-robot relation, and dropped <code class="language-plaintext highlighter-rouge">start_pose</code> from scenes. It’s a better model — the number of poses is the number of robots, so the count and the placement can never disagree. They also independently fixed a dict-aliasing bug I’d found in <code class="language-plaintext highlighter-rouge">views.py</code>, where every instance was sharing one dict and every robot ended up with the last pose — which is exactly why my two drones had been spawning on top of each other.</p> <hr/> <h2 id="making-n-actually-mean-n">Making N actually mean N</h2> <p>The last piece was removing all the hardcoded twos. The browser now scans the exercise’s helper folders for any directory carrying its own copy of the entrypoint, and sends an entrypoint list. RAM launches one process per entrypoint and names each agent after its folder:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/workspace/code/academy.py           -&gt; academy   (the student's cat)
/workspace/code/drone_1/academy.py   -&gt; drone_1   (the mouse)
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">processA</code> / <code class="language-plaintext highlighter-rouge">processB</code> are gone as concepts. Adding a third robot is now two data edits — one more pose in the database, one more folder in the exercise — with no code change in RAM or the frontend.</p> <hr/> <p>The pipeline is cleaner than it was at the start of the week, and the assumptions are a lot more explicit. Two drones, one manager, and N is actually N now.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><category term="gazebo"/><summary type="html"><![CDATA[Week 12 — finding the parts of the pipeline quietly held together by assumptions, and fixing them one by one.]]></summary></entry><entry><title type="html">Polishing, PRs, and a Lot of Code Review</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/polishing-prs-and-code-review/" rel="alternate" type="text/html" title="Polishing, PRs, and a Lot of Code Review"/><published>2026-07-15T06:30:00+00:00</published><updated>2026-07-15T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/polishing-prs-and-code-review</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/polishing-prs-and-code-review/"><![CDATA[<p>This week felt different from the last few. Not a lot of new things being built — more like going back over everything that exists and making it actually good. Testing, tuning, and getting the work review-ready across all four repositories.</p> <hr/> <h2 id="making-the-chase-actually-look-like-a-chase">Making the chase actually look like a chase</h2> <p>The algorithm already worked, but “works” and “looks good in a demo” are two different things. I spent time tuning the guidance so the cat closes in smoothly instead of overshooting and wobbling around the mouse. Small adjustments, but the exercise now reads like a real excersice.</p> <hr/> <h2 id="cameras-the-cat-only">Cameras: the cat only</h2> <p>Reworked the camera setup so the student only gets access to the cat’s own cameras — frontal and ventral views, showing up on the GUI. There’s deliberately no way to peek through the mouse’s cameras. The cat has to work with what it can see and where the mouse actually is — not the mouse’s point of view. Felt like the right call for an exercise.</p> <hr/> <h2 id="reference-solution">Reference solution</h2> <p>Updated the reference solution to match the current ROS 2 version of the exercise. The old one was written for the ROS 1 vision pipeline — it referenced APIs and a camera setup that no longer exist. The new one uses the predictive-intercept guidance law and displays the cat’s cameras exactly like a student’s solution would, so it’s actually usable as a reference.</p> <hr/> <h2 id="the-pr-round">The PR round</h2> <p>The biggest chunk of the week was pull requests and code review. The work spans four repositories — RoboticsApplicationManager, RoboticsAcademy, RoboticsInfrastructure, and RoboticsAcademy-solutions — so I split the changes into focused PRs for each, one concern at a time.</p> <p>Then came the review. Javier left a lot of detailed feedback, and I worked through every comment:</p> <ul> <li>Reverting exercise-specific logic out of the generic manager — the manager should stay reusable, exercise quirks belong in the exercise layer</li> <li>Fixing how robot configs and launchers are tracked — a world without a robot sends a dummy config that has no launcher, and that case now gets handled cleanly instead of silently</li> <li>Moving entity-uniquifying logic into the right layer, applied only when names actually clash</li> <li>A lot of smaller cleanups — removing defensive code that should just fail loudly, inlining tiny functions that didn’t earn their own abstraction.</li> </ul> <p>Honestly this review round taught me more than I expected. The push toward keeping the core generic and letting things fail loudly instead of silently patching over them — that’s the kind of feedback that actually changes how you write code going forward, not just for this PR.</p> <hr/>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><summary type="html"><![CDATA[Less building, more sharpening — tuning the chase, reworking cameras, and working through a detailed review cycle across four repos.]]></summary></entry><entry><title type="html">A Bit of a Mixed Bag (But a Good One)</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/algorithms-video-and-n-robots/" rel="alternate" type="text/html" title="A Bit of a Mixed Bag (But a Good One)"/><published>2026-07-07T06:30:00+00:00</published><updated>2026-07-07T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/algorithms-video-and-n-robots</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/algorithms-video-and-n-robots/"><![CDATA[<p>This week was two very different halves. One was deep in guidance algorithms and robotics math. The other was me basically becoming a part-time video editor. Both ate way more time than I expected — but in a good way.</p> <hr/> <h2 id="building-out-the-chase-algorithms">Building out the chase algorithms</h2> <p>Last week the cat could sort of follow the mouse. This week I wanted it to actually mean something, so I built out a proper ladder of pursuit algorithms.</p> <p>Started with plain <strong>pure pursuit</strong> — just aim at the target and go. Then <strong>proportional navigation</strong>, where the cat leads the target instead of chasing its tail. Then <strong>augmented PN</strong>, and finally a <strong>predictive intercept</strong> that solves for where the two drones will actually meet given their current velocities. I also added a <strong>visual servoing</strong> version that chases using only the camera feed — no coordinates, no GPS.</p> <p>The mouse got a real upgrade too. I gave it eight difficulty levels, from slow and predictable up to fast racing lines, zig-zags, spirals, and figure-eights. I tuned them on purpose so each level quietly breaks the previous algorithm, which makes the next one feel like the natural fix. The “failure is the lesson” structure turned out really well.</p> <p>On the student side, I pushed all the boilerplate — camera setup, recovery logic, crash handling — into the backend so a student only writes the actual chasing logic against a clean API.</p> <hr/> <h2 id="the-video">The video</h2> <div style="position:relative;padding-bottom:56.25%;height:0;overflow:hidden;"> <iframe src="https://www.youtube.com/embed/Jj9ORzrbMdQ" style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" allowfullscreen=""></iframe> </div> <p>Then came the video, which kind of took over my week.</p> <p>I had the clips — the chase, play/pause/reset, everything in motion — but stitching it into something that looks good is a different skill. I made an animated intro and title card with the GSoC × JdeRobot, built diagrams for the IBVS and PN math (and double-checked the equations are actually correct, not just pretty), designed a thumbnail, and wrote the titles and description from the organisation’s point of view.</p> <p>Then there were a hundred small fiddly things — colors, fixing a BGR/RGB swap on the camera feed, getting the drone to look the way I wanted in frame. The kind of work where you spend an hour and have almost nothing visible to show for it, but it adds up. The final thing looks clean.</p> <hr/> <h2 id="closing-the-mentor-feedback-loop">Closing the mentor feedback loop</h2> <p>Javier had flagged earlier that instead of adding a separate <code class="language-plaintext highlighter-rouge">robots</code> variable, the existing <code class="language-plaintext highlighter-rouge">robot</code> field should just become the array — one field, one source of truth. This week I went and did that properly, top to bottom: <code class="language-plaintext highlighter-rouge">views.py</code>, the frontend API, the IDE interface, and the manager in RAM. Now <code class="language-plaintext highlighter-rouge">robot</code> is a list everywhere. Single-robot exercises are just a list of one, and nothing in the codebase treats it as a singular object anymore. Felt good to close that cleanly.</p> <hr/> <h2 id="cracking-the-parallel-spawn">Cracking the parallel spawn</h2> <p>The N-robot spawning worked, but sequentially — each robot had to fully come up before the next one started. Since reset respawns the whole group, every reset was slow too. We’d tried parallelising it before and it just hung, and nobody was quite sure why.</p> <p>This week I sat down and traced it properly. The launch was never the problem — the waiting was. Each robot was polling Gazebo with a blocking call to check if it had spawned, and when you run that inside worker threads, the blocking call holds Python’s GIL and starves all the other threads. Classic.</p> <p>The fix was to stop threading it entirely. I split the logic into two phases: <strong>start the launch</strong> (fast, fire and forget) and <strong>wait until it spawns</strong> (the slow poll). The manager now starts all the launches first, then waits for all of them together on the main thread. No threads, no GIL contention. Robots come up in roughly one spawn time instead of N, and reset got the exact same speedup for free since it goes through the same path.</p> <hr/> <h2 id="where-things-stand">Where things stand</h2> <p>The N-robots refactor is almost there, the mentor comments are resolved, the algorithms are in good shape, and the video is nearly done. Next week is finishing and verifying the N-robot changes end to end, a couple of small cleanups, and drafting the pr’s.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><category term="aerostack2"/><summary type="html"><![CDATA[Chase algorithms, difficulty levels, a deep dive into video production, and finally cracking the N-robot parallel spawn.]]></summary></entry><entry><title type="html">N Robots in RAM — Testing It End to End</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-in-ram-testing-end-to-end/" rel="alternate" type="text/html" title="N Robots in RAM — Testing It End to End"/><published>2026-06-30T06:30:00+00:00</published><updated>2026-06-30T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-in-ram-testing-end-to-end</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-in-ram-testing-end-to-end/"><![CDATA[<p>Last week I laid out the design — turn RAM’s single robot launcher into a list, fan out every lifecycle action over that list. This week I actually wired it through the full stack (database → Django → React → ide-interface → RAM), tested it with two F1 cars on a circuit, and hit every bug the system had to offer. Here’s the full story.</p> <hr/> <h2 id="where-i-started">Where I started</h2> <p>The <strong>N users</strong> side was done — I had fully tested it and the PRs got merged successfully. That part was solved.</p> <p>What it did <em>not</em> do was put <strong>N robots inside one simulation</strong>. One world, one robot. If I wanted two cars on the same track, or a cat drone chasing a mouse drone, the only way was to hand-edit the world file and bolt the second robot in manually. No automation, no config, no way for the manager to know there was more than one robot to launch, pause, reset, or kill.</p> <p>This is the story of moving from <em>N users</em> to <em>N robots</em> — the bugs I hit, how I chased them down, and how the final design ended up clean.</p> <hr/> <h2 id="the-mental-model-first">The mental model first</h2> <p>Before I get into the bugs, let me lay out the shape of the system. A robot showing up in the simulation is the end of a chain that starts in the database. There are basically five layers, and <strong>only one of them needed to grow a loop</strong>:</p> <ol> <li><strong>Database</strong> — a <em>universe</em> points at one robot <em>type</em> and a world. The world carries a <code class="language-plaintext highlighter-rouge">start_pose</code>. I decided: <strong>N poses in <code class="language-plaintext highlighter-rouge">start_pose</code> = N robots.</strong> No new robot table, no per-robot rows. Just a list of poses.</li> <li><strong><code class="language-plaintext highlighter-rouge">views.py</code></strong> (<code class="language-plaintext highlighter-rouge">get_docker_universe_data</code>) — reads those poses, and instead of returning one robot, returns a <code class="language-plaintext highlighter-rouge">robots</code> <strong>list</strong>: same type for every entry, but each gets its own <code class="language-plaintext highlighter-rouge">entity</code> (<code class="language-plaintext highlighter-rouge">f1_0</code>, <code class="language-plaintext highlighter-rouge">f1_1</code>, …) and its own single pose.</li> <li><strong><code class="language-plaintext highlighter-rouge">api.ts</code></strong> (React frontend) — forwards <code class="language-plaintext highlighter-rouge">universe.robots</code> through to the launch message.</li> <li><strong><code class="language-plaintext highlighter-rouge">jderobot-ide-interface</code></strong> — the package that actually assembles the <code class="language-plaintext highlighter-rouge">launch_world</code> websocket message. It had to learn to carry the <code class="language-plaintext highlighter-rouge">robots</code> list.</li> <li><strong>RAM (the manager)</strong> — receives <code class="language-plaintext highlighter-rouge">launch_world</code>, and <strong>this is the only place that loops.</strong> One launcher per robot, spawned one after another.</li> </ol> <p>The reason I kept the loop in exactly one place is the finite state machine. RAM is a state machine (<code class="language-plaintext highlighter-rouge">idle → connected → world_ready → tools_ready → application_running → paused</code>). I did <strong>not</strong> want to touch the states. So instead of a single <code class="language-plaintext highlighter-rouge">robot_launcher</code> and a single <code class="language-plaintext highlighter-rouge">robot_config</code>, RAM now holds a <strong>list</strong> of launchers and a <strong>list</strong> of configs, and every lifecycle action — launch, reset, pause, terminate — just fans out over the list. The states never knew anything changed.</p> <p>That symmetry is the whole trick: the same way RAM already handled a <em>group of agents</em> (N user applications) on the software side, it now handles a <em>list of robots</em> on the simulation side. Same idea, mirrored.</p> <hr/> <h2 id="the-bugs-in-the-order-they-hurt">The bugs, in the order they hurt</h2> <h3 id="1-the-robot-list-vanished-before-it-ever-reached-ram">1. The robot list vanished before it ever reached RAM</h3> <p>First test with two robots: only one car spawned. The database had two poses. <code class="language-plaintext highlighter-rouge">views.py</code> was correctly building a two-entry <code class="language-plaintext highlighter-rouge">robots</code> list. But the second car simply never existed.</p> <p>The list was being <strong>dropped on the way through the frontend.</strong> <code class="language-plaintext highlighter-rouge">api.ts</code> returned <code class="language-plaintext highlighter-rouge">world</code>, <code class="language-plaintext highlighter-rouge">robot</code>, <code class="language-plaintext highlighter-rouge">tools</code> — but not <code class="language-plaintext highlighter-rouge">robots</code>. And even after I added it there, the <code class="language-plaintext highlighter-rouge">jderobot-ide-interface</code> package was assembling the final <code class="language-plaintext highlighter-rouge">launch_world</code> message <em>without</em> the <code class="language-plaintext highlighter-rouge">robots</code> field. Two separate layers were silently discarding it.</p> <p><strong>How I debugged it:</strong> I logged the actual <code class="language-plaintext highlighter-rouge">launch_world</code> payload arriving at RAM. The <code class="language-plaintext highlighter-rouge">robots</code> key wasn’t there at all — which meant the problem was upstream of RAM, not inside it. Then I walked the value backwards: present in <code class="language-plaintext highlighter-rouge">views.py</code>, present after <code class="language-plaintext highlighter-rouge">api.ts</code>, <em>gone</em> after <code class="language-plaintext highlighter-rouge">ide-interface</code>. That pinned the culprit.</p> <p><strong>Fix:</strong> I added <code class="language-plaintext highlighter-rouge">robots: response.data.universe.robots</code> in <code class="language-plaintext highlighter-rouge">api.ts</code>, and patched <code class="language-plaintext highlighter-rouge">ide-interface</code> to copy <code class="language-plaintext highlighter-rouge">robots: universeConfig.robots</code> into the <code class="language-plaintext highlighter-rouge">universe_config</code> it builds. (The <code class="language-plaintext highlighter-rouge">ide-interface</code> fix is a separate JdeRobot package — locally it’s a <code class="language-plaintext highlighter-rouge">node_modules</code> patch, and the real fix has to land upstream.)</p> <h3 id="2-spawning-robots-in-parallel-hung-the-whole-launch">2. Spawning robots in parallel hung the whole launch</h3> <p>The naive optimization I tried: if I have N robots, spawn them all at once in threads. I wrote a <code class="language-plaintext highlighter-rouge">_run_robots_parallel</code> that fired each launcher on its own thread.</p> <p>It hung. The launch never finished, the state machine sat in <code class="language-plaintext highlighter-rouge">world_ready</code> forever.</p> <p>The launch path is not thread-safe — <code class="language-plaintext highlighter-rouge">ros2 launch</code>, the gz spawn handshake, the bridge bring-up all stepping on each other. <strong>Fix: I reverted to sequential.</strong> <code class="language-plaintext highlighter-rouge">_run_all_robots()</code> just loops and runs each launcher one after the other, waiting for each robot’s entity to actually appear in the scene before moving to the next. Slower, but correct, and “correct” wins when the alternative is a deadlock.</p> <h3 id="3-two-poses-on-a-real-exercise-broke-that-exercise">3. Two poses on a <em>real</em> exercise broke that exercise</h3> <p>This is the one that ate a full day. After the multi-robot plumbing was in, <em>follow_line</em> — a normal single-car exercise — started failing. The car spawned but the student HAL couldn’t talk to it.</p> <p>I was <em>sure</em> it was the new code. It wasn’t. The root cause: I had set <strong>Simple Circuit (world 23)</strong>, follow_line’s actual production world, to <strong>two poses</strong> while testing. Two poses means <code class="language-plaintext highlighter-rouge">views.py</code> names the robots <code class="language-plaintext highlighter-rouge">f1_0</code> and <code class="language-plaintext highlighter-rouge">f1_1</code>. But the follow_line HAL is hard-wired to talk to <code class="language-plaintext highlighter-rouge">/f1/</code>. Rename the entity to <code class="language-plaintext highlighter-rouge">f1_0</code> and every topic the student code expects (<code class="language-plaintext highlighter-rouge">/f1/cmd_vel</code>, <code class="language-plaintext highlighter-rouge">/f1/odom</code>, camera) is suddenly under <code class="language-plaintext highlighter-rouge">/f1_0/</code> instead. The car was fine. The <em>namespace</em> moved out from under the HAL.</p> <p><strong>Fix, and the lesson:</strong> never repurpose a real exercise’s world for multi-robot testing. I reverted world 23 to a single pose and created a <strong>dedicated “F1 Two Cars Test” universe (id 73)</strong> for all two-car work. Rule of thumb I’ve baked into my notes now: <em>a real exercise world must always have exactly the number of poses its HAL expects.</em></p> <h3 id="4-namespacing--making-n-robots-not-stomp-on-each-other">4. Namespacing — making N robots not stomp on each other</h3> <p>Once the list reached RAM and N launchers fired, all N robots published to the <strong>same topics</strong>. Two cars both publishing <code class="language-plaintext highlighter-rouge">/F1ROS/cmd_vel</code>, both spawning a model named <code class="language-plaintext highlighter-rouge">f1</code>, both running a <code class="language-plaintext highlighter-rouge">robot_state_publisher</code> with no namespace. Total collision.</p> <p>This was the actual <em>robotics</em> problem and not just plumbing, so it’s worth explaining properly:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">entity</code> becomes the namespace.</strong> Each robot’s <code class="language-plaintext highlighter-rouge">entity</code> (<code class="language-plaintext highlighter-rouge">f1_0</code>, <code class="language-plaintext highlighter-rouge">f1_1</code>) is used as both its gz model name <em>and</em> its ROS namespace. One value, one identity, everywhere.</li> <li><strong>I render the xacro once per robot</strong>, with a <code class="language-plaintext highlighter-rouge">namespace</code> argument passed in. It’s <em>not</em> a loop inside the xacro — I render the whole robot description fresh for each instance, substituting the namespace. So the camera topic becomes <code class="language-plaintext highlighter-rouge">/$(arg namespace)/cam_f1/image_raw</code>, the laser becomes <code class="language-plaintext highlighter-rouge">/$(arg namespace)/f1/laser/scan</code>, cmd_vel and odom become <code class="language-plaintext highlighter-rouge">/$(arg namespace)/F1ROS/...</code>.</li> <li><strong>I render the bridge YAML per namespace too</strong> — every topic in <code class="language-plaintext highlighter-rouge">f1_renault.yaml</code> gets prefixed with <code class="language-plaintext highlighter-rouge">/&lt;ns&gt;</code> before the ROS↔gz bridge starts.</li> <li><strong>The spawn uses <code class="language-plaintext highlighter-rouge">-name &lt;ns&gt;</code> and <code class="language-plaintext highlighter-rouge">-topic /&lt;ns&gt;/robot_description</code></strong>, and <code class="language-plaintext highlighter-rouge">robot_state_publisher</code> runs under <code class="language-plaintext highlighter-rouge">namespace=ns</code>.</li> </ul> <p>Result: <code class="language-plaintext highlighter-rouge">/f1_0/...</code> and <code class="language-plaintext highlighter-rouge">/f1_1/...</code>, fully isolated, no collisions. <code class="language-plaintext highlighter-rouge">ros2 node list</code> shows each car with its own <code class="language-plaintext highlighter-rouge">robot_state_publisher</code>, <code class="language-plaintext highlighter-rouge">ros_gz_bridge</code>, and <code class="language-plaintext highlighter-rouge">ros_gz_image</code>. I rewrote the launch file to take <code class="language-plaintext highlighter-rouge">entity</code> as a launch argument and thread it through all of the above.</p> <p>RAM passes this down with a single addition in <code class="language-plaintext highlighter-rouge">launcher_robot_ros2_api.py</code>: <code class="language-plaintext highlighter-rouge">entity:=&lt;entity&gt;</code> on the <code class="language-plaintext highlighter-rouge">ros2 launch</code> command. I checked that this is safe even for robots whose launch files don’t declare <code class="language-plaintext highlighter-rouge">entity</code> — <code class="language-plaintext highlighter-rouge">ros2 launch</code> ignores undeclared arguments — so it didn’t break any existing single-robot exercise.</p> <h3 id="5-reset-with-n-robots-would-have-segfaulted">5. Reset with N robots would have segfaulted</h3> <p>This was the subtlest design question, and it’s why the <em>list of launchers</em> matters and not just a list of configs.</p> <p>I can’t reset robots by telling gz to reset the world. Runtime-spawned entities don’t survive a gz world reset cleanly — it <strong>segfaults</strong>. The way single-robot reset already worked was: the <code class="language-plaintext highlighter-rouge">robot_launcher</code> runs the ROS launch on <em>start</em>, and runs it <em>again</em> on reset, respawning the robot fresh. So reset is really “tear down, then re-launch.”</p> <p>For N robots, the only honest generalization is: keep a <strong>list</strong> of robot launchers. On reset:</p> <ol> <li>Loop the list and <strong>terminate every robot.</strong></li> <li>Do the world reset (now safe — nothing runtime-spawned is left to choke on).</li> <li>Loop the list and <strong>re-launch every robot</strong> via <code class="language-plaintext highlighter-rouge">_run_all_robots()</code>.</li> </ol> <p>If I’d only kept a list of <em>configs</em> and a single launcher, I couldn’t tear down and rebuild each robot independently. The launcher <em>is</em> the per-robot handle. So <code class="language-plaintext highlighter-rouge">manager.py</code> holds <code class="language-plaintext highlighter-rouge">self.robot_launchers = []</code> and <code class="language-plaintext highlighter-rouge">self.robot_configs = []</code>, and <code class="language-plaintext highlighter-rouge">reset_sim</code>, <code class="language-plaintext highlighter-rouge">terminate_universe</code>, <code class="language-plaintext highlighter-rouge">disconnect</code>, and <code class="language-plaintext highlighter-rouge">shutdown</code> all just loop the list. The FSM’s <code class="language-plaintext highlighter-rouge">reset</code> rule never changed — it still calls one <code class="language-plaintext highlighter-rouge">reset_sim</code>; that method just does N things now.</p> <hr/> <h2 id="how-it-finally-works">How it finally works</h2> <p>Two F1 cars, on <code class="language-plaintext highlighter-rouge">follow_line</code>, via the “F1 Two Cars Test” universe:</p> <ol> <li><strong>DB</strong>: universe 73 → world with <strong>two poses</strong> in <code class="language-plaintext highlighter-rouge">start_pose</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">views.py</code></strong> reads two poses → builds <code class="language-plaintext highlighter-rouge">robots = [{entity: f1_0, pose: …}, {entity: f1_1, pose: …}]</code>, same type for both. (<code class="language-plaintext highlighter-rouge">robot = robots[0]</code> is kept for backward compatibility with single-robot exercises.)</li> <li><strong><code class="language-plaintext highlighter-rouge">api.ts</code></strong> forwards <code class="language-plaintext highlighter-rouge">robots</code>. <strong><code class="language-plaintext highlighter-rouge">ide-interface</code></strong> packs it into <code class="language-plaintext highlighter-rouge">universe_config</code>. The <code class="language-plaintext highlighter-rouge">launch_world</code> message arrives at RAM carrying the full list.</li> <li><strong>RAM <code class="language-plaintext highlighter-rouge">on_launch_world</code></strong> reads <code class="language-plaintext highlighter-rouge">robots</code>, builds one <code class="language-plaintext highlighter-rouge">LauncherRobot</code> per entry into <code class="language-plaintext highlighter-rouge">self.robot_launchers</code>, then <code class="language-plaintext highlighter-rouge">_run_all_robots()</code> spawns them <strong>sequentially</strong>, each waiting until its entity is in the scene.</li> <li>Each launch renders the <strong>f1 xacro and bridge YAML with its own namespace</strong> (<code class="language-plaintext highlighter-rouge">entity</code>), spawns the model, brings up a namespaced bridge and state publisher.</li> <li>Result: <code class="language-plaintext highlighter-rouge">/f1_0/*</code> and <code class="language-plaintext highlighter-rouge">/f1_1/*</code>, two cars on the track, no collisions. Pause, resume, reset, and shutdown all loop the list. The state machine is untouched.</li> </ol> <p>I staggered the two starting poses along the straight (same lane, 3 m apart in the track direction) so both cars sit on the road instead of one ending up in the grass.</p> <hr/> <h2 id="whats-left">What’s left</h2> <ul> <li>Right now spawning is <strong>sequential</strong> — works fine for two robots, but it won’t scale. Imagine 1000 robots spawning one after another. Next I want to figure out the thread-safety issues and move this to <strong>parallel</strong> spawning.</li> <li>The current design ships <strong>one robot type for all N robots</strong>, which matches my immediate use cases (two identical cars, two drones). Mixed types per universe would be a future extension.</li> </ul> <hr/>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-robot"/><category term="namespacing"/><summary type="html"><![CDATA[Last week I designed the N-robots list approach. This week I wired it through the full stack, tested it with two F1 cars, and fought through every bug along the way.]]></summary></entry><entry><title type="html">Moving from N Users to N Robots</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-support/" rel="alternate" type="text/html" title="Moving from N Users to N Robots"/><published>2026-06-22T06:30:00+00:00</published><updated>2026-06-22T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-support</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/n-robots-support/"><![CDATA[<p>This week the work shifted from supporting multiple users to supporting multiple robots — making RAM symmetric on both sides. Here’s where things stand.</p> <hr/> <h2 id="where-we-started-n-applications-agentgroup">Where we started: N applications (AgentGroup)</h2> <p>A couple of weeks back I added support for running multiple user applications at once — the cat-and-mouse exercise needs two programs running together (the cat the student writes, and a pre-programmed mouse). Instead of teaching RAM’s state machine about “two agents,” I wrapped the agent processes in a small <code class="language-plaintext highlighter-rouge">AgentGroup</code>. The state machine still fires one trigger — play, pause, reset, stop — and the group fans that operation out over every process inside it.</p> <p>So one application or five, the lifecycle code is the same: <code class="language-plaintext highlighter-rouge">pause_all</code>, <code class="language-plaintext highlighter-rouge">resume_all</code>, <code class="language-plaintext highlighter-rouge">kill_all</code>. The finite state machine never had to change. That kept things clean and meant single-agent exercises kept working exactly as before.</p> <hr/> <h2 id="cleaning-up-the-pr-review">Cleaning up the PR review</h2> <p>The cat-and-mouse PRs went through review and a few good points came back from the maintainers. The big one: the drone was being defined inline inside the world file (hundreds of lines), and the mentors wanted it as its own model that the world just includes.Fixed moved the drone model out into its own files, and the world file dropped from ~370 lines to ~75.</p> <p>There was also a round of merge conflicts as upstream kept moving — mostly database id clashes (a new exercise upstream grabbed the same id as mine), which I resolved by bumping the cat-mouse ids out of the way.</p> <hr/> <h2 id="now-n-robots">Now: N robots</h2> <p>RAM handles N user programs, but robots were still added by hand, one at a time, in the world file. So I started on the robot side.</p> <p>The approach mirrors the agent one, but the mentors suggested keeping it as a list rather than a wrapper class: turn the single robot launcher + config into a list of robot launchers + configs. Each robot in the list has its own entity, <code class="language-plaintext highlighter-rouge">launch_file_path</code>, and <code class="language-plaintext highlighter-rouge">start_pose</code>, while the robot type stays shared across all of them. The manager just loops the list. And since spawning N robots one by one is slow, the spawn runs in parallel — a thread per robot.</p> <hr/> <h2 id="the-tricky-part-gazebo-reset">The tricky part: Gazebo reset</h2> <p>The interesting problem here is reset. If you spawn a robot into Gazebo at runtime and then call the world reset, Gazebo wipes anything that wasn’t in the world at load time — so the robot disappears and never comes back. And you can’t reset the robot entity directly either; doing that segfaults.</p> <p>The clean answer is already baked into how RAM is structured: robots are spawned by the <code class="language-plaintext highlighter-rouge">robot_launcher</code>, and RAM re-runs the <code class="language-plaintext highlighter-rouge">robot_launcher</code> on reset. So reset becomes: kill each robot’s launcher → reset the (now empty) world → relaunch each robot. The robot comes back fresh, no direct entity reset, no segfault. Moving from one robot launcher to a list of them means this same proven reset flow just loops over all N robots — nothing about the logic changes, it only fans out.</p> <hr/> <h2 id="whats-next">What’s next</h2> <ul> <li>The robots config comes from the database — so the next piece is letting a universe describe several robots (shared type, per-robot entity + start pose) and the backend handing RAM a robots list.</li> <li>Testing with the F1 car on a circuit (simpler than drones — no full autonomy stack), spawning two cars and checking play / pause / reset all behave for both. The end goal is full symmetry — N applications running against N robots, all driven from config, with the state machine left untouched.</li> </ul>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><summary type="html"><![CDATA[RAM now handles multiple user applications — this week the work shifted to making the robot side symmetric too.]]></summary></entry><entry><title type="html">Getting the Drone Cat-and-Mouse Exercise into Robotics Academy</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/drone-cat-mouse-integration/" rel="alternate" type="text/html" title="Getting the Drone Cat-and-Mouse Exercise into Robotics Academy"/><published>2026-06-13T06:30:00+00:00</published><updated>2026-06-13T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/drone-cat-mouse-integration</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/drone-cat-mouse-integration/"><![CDATA[<p>I’ve been working on this integration for the past few weeks — getting the drone cat-and-mouse exercise properly wired into Robotics Academy has been one of the bigger pieces of the project. Along the way I ran into a bunch of bugs, some obvious and some not at all, and I wanted to document them here as I worked through them.</p> <p>The goal was straightforward: get the exercise running inside Robotics Academy, wired into RAM, launchable from the UI. In reality that meant seeding the database, fixing the launch files, and working through bugs that only reveal themselves once both drones are actually running at the same time.</p> <p>Here’s what it looks like now:</p> <video width="100%" controls=""> <source src="/gsoc2026-Anish_Kumar/assets/video/drone_cat_chase.webm" type="video/webm"/> </video> <hr/> <h2 id="how-exercises-actually-plug-into-ram">How exercises actually plug into RAM</h2> <p>Before getting into the bugs, it helps to understand how Robotics Academy works under the hood. You don’t run exercise code directly — the browser uploads a zip to RAM, and RAM handles everything: launch the world, spawn the robots, start the GUI tools, then run your code as a managed process.</p> <p>For cat-and-mouse I used RAM’s two-process model that we built in the previous weeks. The <strong>cat</strong> is the student’s code (<code class="language-plaintext highlighter-rouge">academy.py</code>), and the <strong>mouse</strong> is a pre-programmed agent RAM launches from a <code class="language-plaintext highlighter-rouge">processB/</code> folder. RAM extracts the zip into <code class="language-plaintext highlighter-rouge">/workspace/code</code>, starts <code class="language-plaintext highlighter-rouge">academy.py</code> as agentA, and if <code class="language-plaintext highlighter-rouge">processB/academy.py</code> exists it starts that as agentB.</p> <h2 id="getting-the-database-to-recognise-the-exercise">Getting the database to recognise the exercise</h2> <p>The first step before anything else is seeding the database — if Robotics Academy doesn’t know the exercise exists, nothing else matters. I wrote an idempotent <code class="language-plaintext highlighter-rouge">copy_drone_cat_mouse.sh</code> script that creates the full chain of linked rows: an <strong>exercise</strong> row (<code class="language-plaintext highlighter-rouge">id=27</code>, <code class="language-plaintext highlighter-rouge">exercise_id='drone_cat_mouse'</code>, tagged <code class="language-plaintext highlighter-rouge">MULTI-AGENT</code>), a <strong>world</strong> row pointing at the launch file, a <strong>universe</strong> row linking the world, an <strong>exercises_universes</strong> row marking it the default, and <strong>exercises_tools</strong> enabling the console, simulator, and web GUI. The script ends with a join query that prints the whole chain so I can see in one glance that the exercise actually resolves to the right launch file.</p> <h2 id="one-rule-that-kept-things-sane">One rule that kept things sane</h2> <p>Don’t touch the shared packages. <code class="language-plaintext highlighter-rouge">drone_wrapper.py</code>, the RAM state machine, and the AS2 stack are used by every other drone exercise. Any change I made there could break something that was already working. So I kept all exercise-specific logic inside cat-and-mouse’s own <code class="language-plaintext highlighter-rouge">HAL.py</code> files and made any RAM edits exercise-agnostic — changes that work for N agents and any world name, not changes hardcoded for this one exercise.</p> <hr/> <h2 id="the-bugs-in-the-order-they-hit-me">The bugs (in the order they hit me)</h2> <h3 id="the-second-drone-was-driving-the-first-drones-motors">The second drone was driving the first drone’s motors</h3> <p>Both drones armed. Both showed FLYING. Rotors on both were spinning. But drone1 just sat there on the ground and didn’t move.</p> <p>I ran <code class="language-plaintext highlighter-rouge">ros2 node info /drone1/platform</code> and looked at its publishers. It was publishing to <code class="language-plaintext highlighter-rouge">/gz/drone0/cmd_vel</code> — drone0’s Gazebo plugin — not its own <code class="language-plaintext highlighter-rouge">/gz/drone1/cmd_vel</code>. Drone1 was working fine, it was just sending all its effort to the wrong place.</p> <p>After digging through the aerostack2 launch files and spending some time searching through ROS2 launch system docs and community threads, I found the cause. The <code class="language-plaintext highlighter-rouge">platform_gazebo_launch.py</code> declares <code class="language-plaintext highlighter-rouge">cmd_vel_topic</code> via <code class="language-plaintext highlighter-rouge">DeclareLaunchArgument</code> inside an <code class="language-plaintext highlighter-rouge">OpaqueFunction</code>. What I didn’t realise is that all included launch files share one global <code class="language-plaintext highlighter-rouge">LaunchContext</code> — it’s essentially a single dictionary for the entire launch tree. <code class="language-plaintext highlighter-rouge">DeclareLaunchArgument</code> behaves like <code class="language-plaintext highlighter-rouge">dict.setdefault</code>: it only sets the value if the key doesn’t already exist. So drone0 runs first, writes <code class="language-plaintext highlighter-rouge">cmd_vel_topic = /gz/drone0/cmd_vel</code> into the dict, and when drone1 tries to register its own value, the key already exists and the assignment is silently skipped. Drone1 ends up with drone0’s topic.</p> <p>This is genuinely easy to miss because the ROS2 runtime namespace system (what gives you <code class="language-plaintext highlighter-rouge">/drone0/</code> and <code class="language-plaintext highlighter-rouge">/drone1/</code> isolated topics and nodes) works completely correctly. It’s only the launch argument dict that has this shared-global behaviour. Most people never hit it because multi-robot setups usually run each robot as a separate <code class="language-plaintext highlighter-rouge">ros2 launch</code> command — separate processes, separate dicts, no collision.</p> <p>Fix: I modified <code class="language-plaintext highlighter-rouge">as2_default_gazebo_sim.launch.py</code> to launch <code class="language-plaintext highlighter-rouge">as2_platform_gazebo_node</code> directly from inside an <code class="language-plaintext highlighter-rouge">OpaqueFunction</code>, computing the topic names as plain Python strings and passing them straight into <code class="language-plaintext highlighter-rouge">Node(parameters=[...])</code>. They never enter the global launch dict at all. I’m also raising this upstream as a scalability issue in aerostack2’s Humble launch files.</p> <hr/> <h3 id="the-mouse-process-died-immediately-on-import">The mouse process died immediately on import</h3> <p>As soon as RAM launched agentB, it crashed: <code class="language-plaintext highlighter-rouge">ModuleNotFoundError: No module named 'hal_interfaces'</code>.</p> <p>I checked the extracted folder layout. <code class="language-plaintext highlighter-rouge">hal_interfaces</code> lives at <code class="language-plaintext highlighter-rouge">/workspace/code/</code> — but <code class="language-plaintext highlighter-rouge">processB/academy.py</code> runs from <code class="language-plaintext highlighter-rouge">/workspace/code/processB/</code>. Python adds the script’s own directory to <code class="language-plaintext highlighter-rouge">sys.path</code>, not its parent. From <code class="language-plaintext highlighter-rouge">processB/</code>, the shared commons in the parent folder are invisible.</p> <p>Fix: in <code class="language-plaintext highlighter-rouge">manager.py</code> I inject <code class="language-plaintext highlighter-rouge">PYTHONPATH=/workspace/code</code> into agentB’s environment when creating its <code class="language-plaintext highlighter-rouge">Popen</code>. Same import experience as agentA, no changes to the exercise files themselves.</p> <hr/> <h3 id="takeoff-was-throwing-task-got-bad-yield">Takeoff was throwing “Task got bad yield”</h3> <p>Both agents were crashing during <code class="language-plaintext highlighter-rouge">takeoff()</code> with a <code class="language-plaintext highlighter-rouge">RuntimeError: Task got bad yield</code>. The traceback pointed at <code class="language-plaintext highlighter-rouge">asyncio.run()</code> wrapping a state-machine service call. rclpy Futures use <code class="language-plaintext highlighter-rouge">yield self</code> in their <code class="language-plaintext highlighter-rouge">__await__</code>, which asyncio’s event loop doesn’t know how to drive.</p> <p>Fix: in the exercise’s <code class="language-plaintext highlighter-rouge">HAL.py</code> I wrote a small <code class="language-plaintext highlighter-rouge">_call_state_event()</code> that calls the service with <code class="language-plaintext highlighter-rouge">call_async()</code> and polls <code class="language-plaintext highlighter-rouge">future.done()</code> in a plain loop, letting DroneWrapper’s 20 Hz spin thread do the resolving. No asyncio, no crash. The shared <code class="language-plaintext highlighter-rouge">DroneWrapper</code> stays untouched.</p> <hr/> <h3 id="the-cat-reached-flying-but-never-moved-toward-the-mouse">The cat reached FLYING but never moved toward the mouse</h3> <p>This one took the longest to figure out.</p> <p>The cat armed, took off, reached the correct altitude, state machine said FLYING — and then just hovered there. <code class="language-plaintext highlighter-rouge">ros2 topic hz /drone0/motion_reference/twist</code> showed nothing being published. <code class="language-plaintext highlighter-rouge">ros2 topic echo /drone0/controller/info</code> showed the controller stuck in HOVER mode. The process wasn’t dead — I checked <code class="language-plaintext highlighter-rouge">/proc/&lt;pid&gt;/task/*/wchan</code> and the main thread was just sleeping in its loop.</p> <p>To narrow it down, I killed the cat code entirely and ran a small standalone script: create a fresh <code class="language-plaintext highlighter-rouge">DroneWrapper("drone0")</code>, call <code class="language-plaintext highlighter-rouge">set_cmd_vel</code>. It switched to SPEED mode in 0.7 seconds, no problem. So the drone stack was fine. The problem was something specific to the cat exercise setup. The only thing cat had that the working mouse didn’t was a mouse-position subscriber.</p> <p>Here’s what was happening. When I added mouse tracking I gave it a dedicated <code class="language-plaintext highlighter-rouge">MultiThreadedExecutor</code> with <code class="language-plaintext highlighter-rouge">.spin()</code> running in its own thread — the reasoning being “the pose topic publishes at ~80 Hz, better give it a dedicated worker so it never misses an update.” In C++ that would be reasonable. In Python, the GIL means only one thread runs Python at a time. <code class="language-plaintext highlighter-rouge">MultiThreadedExecutor.spin()</code> is a blocking, full-speed loop with no sleep. So that thread was holding the GIL almost continuously, starving DroneWrapper’s 20 Hz background thread — the one that receives <code class="language-plaintext highlighter-rouge">controller/info</code> callbacks. AS2’s motion handler won’t publish velocity commands until its internal <code class="language-plaintext highlighter-rouge">current_mode_</code> (which gets updated by that callback) reads SPEED. With the callback starved, <code class="language-plaintext highlighter-rouge">current_mode_</code> stayed at HOVER, the mode-switch wait timed out after 5 seconds, and <code class="language-plaintext highlighter-rouge">set_cmd_vel</code> returned silently without doing anything. The “stuck in HOVER” symptom was a consequence, not the root cause.</p> <p>Fix: moved the mouse tracker onto the cameras’ existing executor, which uses <code class="language-plaintext highlighter-rouge">spin_once(timeout_sec=0) + time.sleep(1/30)</code>. It yields the GIL every cycle. DroneWrapper’s thread gets to run, <code class="language-plaintext highlighter-rouge">controller/info</code> flows, and <code class="language-plaintext highlighter-rouge">set_cmd_vel</code> publishes immediately. The lesson: it wasn’t the executor type that was the problem, it was the greedy <code class="language-plaintext highlighter-rouge">.spin()</code> with no yield.</p> <hr/> <h3 id="the-cat-was-chasing-a-fixed-point-in-space">The cat was chasing a fixed point in space</h3> <p>Once the cat was actually moving, it flew toward a single fixed location rather than tracking the mouse.</p> <p>I ran <code class="language-plaintext highlighter-rouge">ros2 topic echo /drone1/self_localization/pose</code> — nothing. Added <code class="language-plaintext highlighter-rouge">--qos-reliability best_effort</code> — data started printing. Then <code class="language-plaintext highlighter-rouge">ros2 topic info /drone1/self_localization/pose -v</code> confirmed it: the publisher was BEST_EFFORT. My subscriber was using the default QoS profile which is RELIABLE. Those two are incompatible; zero messages were delivered, and <code class="language-plaintext highlighter-rouge">get_mouse_position()</code> kept returning its initialised default coordinates.</p> <p>Fix: subscribe with <code class="language-plaintext highlighter-rouge">qos_profile_sensor_data</code> (BEST_EFFORT). After that the position updated live with every frame.</p> <hr/> <h3 id="stop-left-both-drones-coasting">Stop left both drones coasting</h3> <p>Pressing Stop in the GUI didn’t actually stop the drones — they kept moving on their last velocity command.</p> <p>The issue was in <code class="language-plaintext highlighter-rouge">on_terminate_application</code>: the order was kill the processes, then pause the simulator. Between those two steps the drones still had momentum from their last command and Gazebo’s physics kept running. Swapping the order — pause the sim first, then kill processes, then reset — freezes everything instantly at the moment Stop is hit. Works for any exercise, not just this one.</p> <hr/> <h3 id="pause-and-reset-were-silently-doing-nothing">Pause and Reset were silently doing nothing</h3> <p>Pause appeared to work but didn’t actually freeze the drones. Reset only reset drone0.</p> <p>I ran <code class="language-plaintext highlighter-rouge">gz service -l</code> and compared it against what the code was calling. The gz control calls in <code class="language-plaintext highlighter-rouge">launcher_gzsim.py</code> were hardcoded to <code class="language-plaintext highlighter-rouge">/world/default/control</code>. This exercise’s world is <code class="language-plaintext highlighter-rouge">my_city_world</code>. The calls were hitting a service that didn’t exist and failing silently. And <code class="language-plaintext highlighter-rouge">reset()</code> was hardcoded to call <code class="language-plaintext highlighter-rouge">/drone0/...</code> services — drone1 was completely ignored.</p> <p>Fix: I added <code class="language-plaintext highlighter-rouge">_find_drone_namespaces()</code> which scans <code class="language-plaintext highlighter-rouge">ros2 service list</code> for all <code class="language-plaintext highlighter-rouge">*/platform/state_machine/_reset</code> services and resets every one — it doesn’t care how many drones are running or what the world is named.</p> <hr/> <h2 id="files-changed">Files changed</h2> <ul> <li><code class="language-plaintext highlighter-rouge">RoboticsAcademy/exercises/drone_cat_mouse/python_template/HAL.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsAcademy/exercises/drone_cat_mouse/python_template/academy.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsAcademy/exercises/drone_cat_mouse/python_template/processB/{HAL.py,academy.py}</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsApplicationManager/.../manager/manager.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsApplicationManager/.../manager/launcher/launcher_gzsim.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsInfrastructure/jderobot_drones/launch/as2_default_gazebo_sim.launch.py</code></li> <li><code class="language-plaintext highlighter-rouge">RoboticsInfrastructure/Launchers/drone_cat_mouse.launch.py</code></li> </ul> <hr/> <h2 id="where-it-stands">Where it stands</h2> <p>The exercise launches cleanly from the Robotics Academy UI, both drones spawn and take off, the cat tracks and chases the live mouse, and Stop / Pause / Reset all behave correctly for both drones.</p> <p>Updated PRs with the latest commits:</p> <ul> <li><strong><a href="https://github.com/JdeRobot/RoboticsAcademy/pull/3872">RoboticsAcademy #3872</a></strong></li> <li><strong><a href="https://github.com/JdeRobot/RoboticsApplicationManager/pull/284">RoboticsApplicationManager #284</a></strong></li> <li><strong><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/742">RoboticsInfrastructure #742</a></strong></li> </ul>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="aerostack2"/><category term="debugging"/><category term="multi-agent"/><summary type="html"><![CDATA[Full integration of a two-drone chase exercise into RAM — database wiring, multi-process launch, and seven bugs debugged in order.]]></summary></entry><entry><title type="html">Multi-Process Code Execution in RoboticsAcademy</title><link href="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/multiprocess-code-execution/" rel="alternate" type="text/html" title="Multi-Process Code Execution in RoboticsAcademy"/><published>2026-06-07T06:30:00+00:00</published><updated>2026-06-07T06:30:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/multiprocess-code-execution</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Anish_Kumar/blog/2026/multiprocess-code-execution/"><![CDATA[<p>This week’s goal was proving that RAM can manage two independent agent processes from a single exercise session — the foundation needed for the Drone Cat-Mouse exercise.</p> <hr/> <h2 id="why-the-browser-sends-the-zip">Why the browser sends the zip</h2> <p>One design decision worth noting: the exercise code — including <code class="language-plaintext highlighter-rouge">processB/</code> — is bundled and sent from the browser, not fetched from a database on the RAM side (which was one option considered).</p> <p>Keeping code in the browser payload means RAM stays decoupled from any storage backend — code lives with the client and gets pushed to RAM at run time. The mentor flagged this as a clean design point for RoboticsAcademy’s architecture.</p> <hr/> <h2 id="how-the-play-flow-works-now">How the Play flow works now</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/gsoc2026-Anish_Kumar/assets/img/architect_image-480.webp 480w,/gsoc2026-Anish_Kumar/assets/img/architect_image-800.webp 800w,/gsoc2026-Anish_Kumar/assets/img/architect_image-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/gsoc2026-Anish_Kumar/assets/img/architect_image.png" class="img-fluid rounded" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Zip merge architecture — how both agents land in /workspace/code/</figcaption> </figure> <p>The browser doesn’t just send the student’s code — it builds a <strong>finalZip</strong> by merging three sources:</p> <ol> <li><strong>userZip</strong> — the student’s <code class="language-plaintext highlighter-rouge">academy.py</code> from the browser editor (cat code, autosaved)</li> <li><strong>helperZip</strong> — fetched from the filesystem: <code class="language-plaintext highlighter-rouge">HAL.py</code>, <code class="language-plaintext highlighter-rouge">WebGUI.py</code>, and the entire <code class="language-plaintext highlighter-rouge">processB/</code> folder (pre-programmed mouse + its own <code class="language-plaintext highlighter-rouge">HAL.py</code>)</li> <li>The two zips are merged client-side into <strong>finalZip</strong> and sent to RAM</li> </ol> <p>RAM receives the merged payload, extracts everything to <code class="language-plaintext highlighter-rouge">/workspace/code/</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/workspace/code/
├── academy.py              ← student code (cat, drone0)
├── HAL.py                  ← drone0 interface
├── hal_interfaces/
├── gui_interfaces/
├── console_interfaces/
└── processB/
    ├── academy.py          ← pre-programmed mouse
    └── HAL.py              ← drone1 interface
</code></pre></div></div> <p>RAM then launches both:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 /workspace/code/academy.py           ← agentA (cat, drone0)
python3 /workspace/code/processB/academy.py  ← agentB (mouse, drone1)
</code></pre></div></div> <p>Both go into the <code class="language-plaintext highlighter-rouge">application_processes</code> dict. Pause sends SIGSTOP to both. Resume sends SIGCONT. Stop kills both.</p> <p>Verified both running simultaneously from their extracted paths:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>11255  python3 /workspace/code/academy.py
11256  python3 /workspace/code/processB/academy.py
</code></pre></div></div> <p>One process for the cat, one for the mouse — confirmed live. Each HAL resolves to a different drone namespace — same import name, different process context, different physical drone.</p> <p>Slowly heading towards the goal.</p> <hr/> <h2 id="what-this-means-for-the-project">What this means for the project</h2> <p>The pattern is exercise-agnostic. Any future exercise needing N agents follows the same structure: add a <code class="language-plaintext highlighter-rouge">processB/</code>, <code class="language-plaintext highlighter-rouge">processC/</code> folder to the exercise template. RAM detects and launches them. The <code class="language-plaintext highlighter-rouge">application_processes</code> dict handles lifecycle for all uniformly.</p> <hr/> <h2 id="pull-requests">Pull Requests</h2> <p>Also drafted and opened PRs this week consolidating all previous work:</p> <ul> <li><strong><a href="https://github.com/JdeRobot/RoboticsApplicationManager/pull/284">RoboticsApplicationManager #284</a></strong></li> <li><strong><a href="https://github.com/JdeRobot/RoboticsInfrastructure/pull/742">RoboticsInfrastructure #742</a></strong></li> </ul> <p>Under review.</p>]]></content><author><name></name></author><category term="weekly-update"/><category term="gsoc"/><category term="jderobot"/><category term="ros2"/><category term="ram"/><category term="multi-agent"/><summary type="html"><![CDATA[Making RAM manage two independent agent processes from a single exercise session — the foundation for Drone Cat-Mouse.]]></summary></entry></feed>